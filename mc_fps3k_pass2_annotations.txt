# FPS-3000 MC annotations — mode: cooperative
0xf04a02 [R1 BOTH] This instruction is part of the channel configuration dispatch logic, comparing the command value in d0 against 0x44 to determine whether to use a stack-based or USP-based method for storing the channel argument into the global variable at $e74.l, which is the per-channel command argument echo used by the XP-32 panel command interface.
0xf04748 [R1 BOTH] This branch (bne.w) checks if bit 7 of the global status byte at $e87.l is set, and if so, jumps to TCBRDHC_ErrorPath — this is the error handling path for the RDHC (Read/Display/Handle Channel) master/dispatch task, which manages XP-32 channel command processing.
0xf04fd6 [R1 BOTH] Dispatches panel command 0x25D (likely EXP-32 reset or initialization) via PanelIOConfigure_25A kernel subroutine.
0xf04ea6 [R1 BOTH] This bclr.b instruction clears bit 7 of the command/status word at $216(a0) (the AP I/F data register high half / Cmd at FF0216), which is part of the XLTR register block — this clears a flag before writing the channel data to the per-channel data port, managing the XP-32 command dispatch handshake.
0xf04dec [R1 BOTH] This move.l loads the 32-bit value from $e58.l (a microcode address or offset) into d1, which is then masked, shifted, and used to compute a destination address in the 0x400000+ range — this is part of the microcode staging buffer address calculation for the XP-32 WCS upload, where the upper bits select the bank and the lower bits form the word offset.
0xf04af8 [R1 BOTH] This move.w copies a word from (a0) to (a1)+, incrementing the destination pointer — this is the core data transfer loop for the XP-32 DMA/microcode upload, copying words from the source buffer to the XLTR data register (FF0214/FF0216) while polling bit 15 of the status register at $218(a5) for ready/done.
0xf049ba [R1 BOTH] This beq.w branches to loc_F04A1E if bit 6 of $e87.l is clear — this is part of the channel configuration dispatch logic that selects between different methods for storing the channel argument (stack-based, USP-based, or direct) based on the status flags, determining how the per-channel command argument is written to $e74.l.
0xf057e0 [R1 BOTH] This btst.b tests bit 13 of d4 (which holds a status/error word from the panel command response) — if set, it triggers sending panel command 0x26a (an abort/error recovery command) and then clears the corresponding error bit in the IRQ mask register at $21a(a4) using the PanelErrorMaskTable lookup, handling XP-32 error recovery.
0xf04930 [R1 BOTH] This movem.l saves all registers (d0-d7/a0-a7) to the stack — this is the entry point for the error/interrupt handler at loc_F04930, which then reads the AP I/F command/status register at $ff0000, clears bit 11, stores the status to $e86.l, sets bit 10 (busy flag), and checks for further error conditions in the XP-32 panel command dispatch.
0xf054a2 [R1 BOTH] This move.l loads a 32-bit value from (a0)+ into d1 — this is part of the S-Record data handler's parsing loop (at loc_F0549E), where consecutive longwords are loaded from the input buffer to extract address and data fields for the microcode staging buffer, with subsequent operations checking that the total length doesn't exceed 0x10 bytes per record.
0xf04784 [R1 BOTH] This instruction calls `PanelIOConfigure_25A` with d0=0x278, which is a panel command that decodes as an Am29116 SUBRC instruction (Group B, TODRA class). This is part of the RDHC (Read/Display/Help/Control) task's main loop, where after a failed TCB definition lookup (trap #1 with d0=0xb), it sends a panel command to the XP-32 to abort or reconfigure the current channel operation.
0xf04776 [R1 BOTH] This loads the address of `TCBDefEntry_RDHC` into a0, preparing to call trap #1 (RMS68K kernel service) to define or look up a Task Control Block for the RDHC task. This is part of the RDHC task initialization sequence in the TCBRDHC_MainLoop, where the firmware checks if the RDHC task's TCB needs to be created before proceeding with XP-32 channel configuration.
0xf0496e [R1 BOTH] This compares d0 against 0x14 (20 decimal), which is a bounds check on a channel number or command code extracted from the global variable at $e86. This is part of the channel configuration dispatch logic, where the firmware validates that the channel index is within the valid range (0-20) before proceeding to configure or send commands to the XP-32's per-channel data ports.
0xf04dbc [R1 BOTH] This is an unconditional branch to `loc_F04E22`, which continues the channel configuration dispatch logic. This instruction is reached after copying a channel's configuration word from the TCB's offset $204 into the global variable $e70 or $e72, depending on whether bit 6 of $e87 is set, as part of the XP-32 per-channel command setup sequence.
0xf04e46 [R1 BOTH] This compares d1 (a count or size value from $e60) against the value at $105e.l, which is likely a maximum buffer size or limit for the microcode staging area. This is part of the S-Record data handler validation, ensuring that the incoming microcode data does not exceed the available space in the 64KB staging buffer (0x10000-0x1FFFF) before sending panel commands to upload it to the XP-32.
0xf056c0 [R1 BOTH] Clears AP I/F status register during XP-32 command initialization in PanelSendAndWait kernel.
0xf04756 [R1 BOTH] This compares d0 (masked to lower 4 bits from $e86) against 0x08, checking if the current channel or operation mode is 8. This is part of the RDHC task's main loop decision logic, where the firmware determines whether to proceed with XP-32 channel configuration or skip to error handling based on the current channel state and the pending panel command code in $e74.
0xf0584a [R1 BOTH] This loads d0 with 0x26c, which is a panel command code that decodes as an Am29116 SUBRC instruction (Group B). This is part of the panel command timeout/error handling in `PanelSendAndWait`, where if the AP I/F status register shows no ready bit (bit 14) and the timeout counter (d5) has expired, it sends an abort command (0x26c) to the XP-32 to cancel the pending operation.
0xf04ce8 [R1 BOTH] This calls `PanelSendAndWait` to send a panel command to the XP-32, using parameters derived from global variables $e60 (channel count/index), $e68 (command argument), and a base address computed from a table at $f046e0 plus 0xFF0000 (AP I/F base). This is part of the channel configuration dispatch, where the firmware sends a per-channel command (likely one of the 0x258-0x27D codes) to configure or control a specific XP-32 channel.
0xf0548e [R1 BOTH] This performs a logical left shift by 1 on d4 (which was decremented from a count value), effectively multiplying the index by 2. This is part of the S-Record data finalization or microcode staging buffer management, where the firmware computes an offset into a data structure (likely a bit table or flag array at offset 0x10a1 from a base address) to clear a bit, indicating completion of processing for a specific microcode block or channel operation.
0xf04dd2 [R1 DS] This branch (bra.w loc_F04E1A) is part of the XP-32 channel configuration dispatch logic, skipping the normal address computation when bit 6 of $e87 indicates a special mode, likely for handling microcode staging buffer address translation or channel-specific parameter setup.
0xf0554c [R1 DS] This conditional branch (bne.b loc_F0555A) is part of the S-record type identification logic in the microcode upload handler, distinguishing between S2/S3 records (16-bit address) and S7/S9 records (termination), controlling how the loader processes the incoming hex data for the XP-32 WCS staging buffer.
0xf04ff6 [R1 DS] This conditional branch (beq.b loc_F04FFE) is part of the channel configuration dispatch loop, checking bit 4 of the status byte at $e87 to determine whether to increment the channel counter, controlling the iteration through XP-32 channels during microcode upload or command dispatch.
0xf04694 [R1 DS] This instruction (ori.b #$50, d0) is DATA — it appears in a region of the ROM that contains RMS68K marker tags and table data (likely part of the TCB or CCB definition table), not executable code, as indicated by the surrounding ASCII patterns and zero words.
0xf04b9e [R1 DS] This conditional branch (bne.b loc_F04BBC) is part of the S-record type dispatch in the microcode upload handler, checking for S1 records (16-bit address) to route data to the SRecordDataHandler for writing to the XP-32 WCS staging buffer at addresses 0x10000-0x1FFFF.
0xf054a4 [R1 DS] This instruction (move.l (a0)+, d2) is part of the panel command argument processing logic, loading a 32-bit value from a data structure (likely an IOV or ASQ entry) to be used as a parameter for the subsequent PanelIOConfigure_25A call with command 0x25B, which decodes as an Am29116 SUBRC instruction for XP-32 communication.
0xf05228 [R1 DS] This subroutine call (jsr PanelIOConfigure_25A.l) sends panel command 0x25A to the XP-32, which decodes as an Am29116 SUBRC instruction (Group A, TORIA), used to signal completion or readiness after the S-record data transfer to the microcode staging buffer.
0xf04b80 [R1 DS] This instruction (addq.l #$1, d0) is part of the S-record parsing loop in the microcode upload handler, incrementing the character pointer after reading a byte from the incoming hex data stream, before calling the hex-to-binary conversion routine at loc_F05150.
0xf04d90 [R1 DS] This instruction (move.l #$400000, d1) is part of the address translation logic for the XP-32 microcode staging buffer, computing a base offset of 4MB (0x400000) to handle memory accesses that exceed the 4MB boundary, likely for mapping into the XP-32's extended address space during channel configuration.
0xf05210 [R1 DS] This branch (bra.b SRecordParseLoop) is part of the S-record data handler, looping back to parse the next byte of the incoming hex record after successfully storing a byte into the microcode staging buffer at addresses 0x10000-0x1FFFF, enforcing the valid address range check.
0xf04966 [R1 BOTH] Compares the masked XLTR Mode 0 register value to zero as part of XLTR configuration during XP-32 hardware initialization.
0xf05370 [R1 BOTH] This instruction saves the base address pointer (a0) into a6 at the start of the SRecordDataHandler's data processing routine, preserving the current position in the microcode staging buffer (0x10000-0x1FFFF) for subsequent byte extraction and checksum accumulation.
0xf0498c [R1 BOTH] Branches if the channel configuration value (from XLTR register) is not 0x10 during PanelIOConfigure_25A dispatch.
0xf052ae [R1 BOTH] This instruction clears the XLTR Data register (FF0218) by writing zero, acknowledging the completion of a DMA transfer from the XP-32 panel bus after polling bit 15 (ready/done flag) in the previous loop.
0xf05244 [R1 BOTH] This instruction tests bit 15 (the ready/done flag) of the XLTR Status/IRQ register at FF0218, polling in a tight loop to wait for the XP-32 panel bus transfer to complete before reading the next data word from the microcode staging buffer.
0xf0a0b6 [R1 BOTH] This instruction tests bit 4 of the TCB's attribute word (d2) to check if the task has the "interrupt-driven" flag set, determining whether to link this TCB into the interrupt service chain at $c14.w during RTOSKernelInit's TCB initialization.
0xf079a0 [R1 BOTH] This instruction writes the opcode 0x8004 (REQUEST-TRANSFER) to the AP I/F command register at FF0000, initiating a panel bus command transaction that triggers the XP-32's Am29116 to execute the corresponding SUBRC microinstruction.
0xf05e40 [R1 BOTH] This instruction stores the modified d1 value (with bit 1 set to indicate a 32-bit transfer) into offset 0x20 of the I/O Vector (IOV) structure pointed to by a4, configuring the transfer size for the TCBIO1I host I/O channel task's DMA operation.
0xf09176 [R1 BOTH] This instruction saves the a0 register on the stack at the entry to PTMInit, preserving the caller's address register before initializing the MC6840 Programmable Timer Module at 0xF70001 with a control byte of 0x01 (reset/initialize the timer).
0xf099c6 [R1 BOTH] Increments the source pointer by 4 in a microcode copy loop from staging buffer to XP-32 control store.
0xf06876 [R1 DS] This `addq.w #1, d3` increments a loop counter in a routine that iterates through 32-byte TCB entries (indexed by d4) checking status bits 0x0F and 0x0E, likely part of the RTOS kernel's task scheduling or TCB scan in `VCTScanSetup` or related dispatch logic.
0xf08872 [R1 DS] This `movea.l #$1f000, a2` sets up address register a2 to point to 0x1F000 (within the upper 64KB RAM region) during hardware initialization, specifically for the RAM addressing test (`RAMAddressingTest`) or memory bus probe (`MemBusProbe`) that validates the microcode staging buffer area.
0xf062d4 [R1 DS] This `move.w (a7)+, d4` pops a value from the supervisor stack into d4 during the IRQ mask manipulation routine at `loc_F062CA`, where d4 indexes into a lookup table at `loc_F0668C` to clear a specific bit in the XLTR IRQ Mask register (0xFF021A), part of the panel command completion/error handling.
0xf09b36 [R1 DS] This `lea.l $404000.l, a1` loads address register a1 with 0x404000 during the PTM initialization (`PTMInit`) or a related timer setup routine, establishing a destination pointer for MC6840 PTM configuration writes via `movep` instructions at the odd-byte-aligned PTM register block (0xF70001-0xF7000F).
0xf07dc6 [R1 DS] This `beq.b loc_F07DD6` branches on a zero comparison result from `cmpi.w #$0, d0`, checking the return status of a `trap #1` (RMS68K system call) that attempted to locate a TCB by name; a zero result triggers the TCBXP1I task entry setup at `loc_F07DD6`, part of the channel task initialization for XP32 channel 1.
0xf0a3b2 [R1 DS] This `bsr.b loc_F0A424` branches to a subroutine during the `Init_GST_StoreTag` or `Init_UST_StoreTag` routine, where a 4-byte marker tag (e.g., "!GST" or "!UST") is being written into the Global/User System Table, and this call likely validates or finalizes the table entry after clearing a block of memory.
0xf0a2aa [R1 DS] This `divu.w #$4, d0` divides the value 0x320 (800 decimal) by 4, yielding 200, during the `RTOSKernelInit` routine's configuration of the MC6840 PTM timer; the result is used to compute timer reload values stored via `movep.w` at offset 0x0D of the PTM base address for periodic interrupt generation.
0xf0862a [R1 DS] This `bra.w loc_F08648` is an unconditional branch in the `IOChannelDiagnostic` function, taken when bit 11 of a status word (at (a0)) is set, indicating a specific channel error condition; the branch skips over normal processing to handle the error path, likely aborting the diagnostic for that channel.
0xf09dd4 [R1 DS] This `andi.w #$f, d1` masks the low nibble of a byte read from a buffer (pointed by a1) during the `SRecordDataHandler` routine, extracting the 4-bit length field from an S-record data line; the high nibble (masked with 0x70) is processed separately to decode the record type.
0xf07110 [R1 DS] This `subq.w #$1, d2` decrements a loop counter (initialized to 0x10) in a routine that iterates through 16 entries of a table at offset 0x1080 from a base address, likely part of the `PanelIOConfigure_25A` command dispatch or channel configuration setup, where d2 controls the number of iterations for processing per-channel data ports.
0xf05416 [R2 BOTH] This instruction computes the base address of a per-channel AP I/F register block by adding zero to a3 (which already holds the channel-specific offset from a dispatch table plus 0xFF0000), effectively finalizing the pointer to the channel's command/data port region for the subsequent PanelSendAndWait call.
0xf05372 [R2 BOTH] This loads d4 from offset 4 of the TCB or control block pointed to by a6, retrieving the channel number or configuration parameter that will be validated against bounds (1 to value at 0x105e) before dispatching a panel command.
0xf04cf2 [R2 BOTH] This tests bit 6 of the byte at 0xe87, which is a configuration flag controlling whether the channel's mode register value is stored to 0xe58 (direct) or 0xe5a (alternate), selecting between two different XLTR Mode 0 register update paths during channel configuration.
0xf04c42 [R2 BOTH] This compares d0 against the value at 0xe64, which is the maximum valid channel number or configuration parameter; if d0 is less than that bound, it branches to a validation-failure handler that issues panel command 0x25f (an abort or error code).
0xf04e30 [R2 BOTH] Increments channel index $e58 when status bit4 indicates pending operation
0xf04ec4 [R2 BOTH] This writes zero to 0xe74, clearing a temporary status or command argument field after a panel command has been issued, ensuring the next command starts with a clean argument state.
0xf04ab4 [R2 BOTH] This writes zero to 0xe74, clearing the command argument field before initiating a new panel command sequence (specifically for command 0x259, which is a channel configuration operation), ensuring no stale data interferes.
0xf0492c [R2 BOTH] This branches unconditionally to loc_F04736, which is the main loop entry of the TCBRDHC task (the master/dispatch task for the RDHC channel), returning control to the task's command processing loop after completing a channel configuration step.
0xf04ee4 [R2 BOTH] This reads the word at offset 0x204 of the AP I/F base (a0), which is the per-channel data port or command argument register, to validate it as a channel number before dispatching panel command 0x25c (a channel-select or configuration command).
0xf05388 [R2 BOTH] This conditional branch (blt) checks if d4 (the channel number loaded from the TCB) is less than 1; if so, it jumps to a handler that issues panel command 0x25c (an error/abort code) because the channel number is out of valid range (1 to value at 0x105e).
0xf0577c [R2 DS] Sends panel command 0x26b (a SUBRC instruction targeting Am29116 register R27) to clear a specific error mask bit in the XP-32's error status register at $21a(a4), part of the error handling path after a panel command completes with error bit 13 set.
0xf057b0 [R2 DS] Sets up a 1000-tick timeout counter (d5 = 0x3e8) for polling the AP I/F command/status register at (a0) after issuing a CONTINUE-TRANSFER (0x8005) command, used in the 32-bit argument follow-up sequence of PanelSendAndWait.
0xf04948 [R2 DS] Sets bit 10 in the AP I/F Mode 0 register (FF0200) via the command/status register at FF0000, part of the XLTR control block configuration sequence — this bit manipulation is part of the VERSAmodule bus interface setup for XP-32 communication.
0xf05654 [R2 DS] Pushes a word value of 2 onto the stack as part of a TRAP #1 system call (RMS68K kernel service), likely setting up parameters for a memory allocation or task creation call related to the XP-32 microcode staging buffer management.
0xf04cee [R2 DS] Branches to ChannelConfigDispatch after calling PanelSendAndWait, which dispatches per-channel configuration based on the channel number stored in $e68.l — this is the main dispatch point after sending a panel command to configure an XP-32 channel.
0xf04da0 [R2 DS] Loads a longword from address (a1) into global variable $e70.l, which is part of the channel configuration data retrieval — specifically reading a configuration table entry indexed by the channel number (after masking to 20 bits and scaling by 4).
0xf05324 [R2 DS] Compares the first longword at address 0x400000 (the XP-32 microcode staging buffer base) against zero to check if the buffer is empty/initialized, part of the microcode upload validation — if the value is ≤0 or >4, it sends panel command 0x259 (a SUBRC instruction) as an error response.
0xf053ac [R2 DS] Computes an address offset from channel number (d4-1 shifted left by 1) and checks bit 1 at offset $10a1 from that base, part of the per-channel status/configuration bit testing — if the bit is clear, it proceeds to send a panel command with a "HXP0" + channel number string via loc_F05652.
0xf04d7e [R2 DS] Masks the channel configuration value in d1 to 20 bits (0xFFFFF) and shifts left by 2 to form a word-aligned index, used to read a configuration table entry from the XP-32's memory-mapped control store at addresses starting from 0x400000 — part of the channel parameter loading sequence.
0xf04b66 [R2 BOTH] This `addq.l #$1, d0` increments a word counter during a loop that reads 16-bit data words from the AP I/F command/data interface at `0xFF0000` (via `a0`), using the XLTR status register at `$218(a5)` (which maps to `0xFF0218`) to poll bit 15 (ready/done) before each read — this is part of a microcode data transfer routine that reads successive 16-bit words from the host interface into the staging buffer.
0xf055a8 [R2 BOTH] Load 16-bit SRecord data from (a0) into staging buffer during microcode upload
0xf05876 [R2 BOTH] This `move.w d0, $21a(a4)` writes an error-masked status word back to the per-channel error status register at offset `$21a` (relative to `a4`, the AP I/F base `0xFF0000`), after clearing a specific error bit indicated by `d5` from `PanelErrorMaskTable` — this is part of the panel command error handling that clears individual error flags after an abort command (0x269) has been issued.
0xf05220 [R2 BOTH] This `move.w (a0), d0` reads a 16-bit value from the address pointed to by `a0` (which is `0xFF0000`, the AP I/F command/status register) in a tight polling loop that waits for the status register to become non-positive (≤0) — this is part of the S-record finalization routine that waits for the AP I/F to become idle before issuing a panel command (0x25A) to finalize the microcode upload.
0xf04906 [R2 BOTH] This `lea.l loc_F0469E.l, a0` loads the address of the RDHC task entry point (`TCBRDHC_Entry`) into `a0` before executing `trap #$1` (which is the RMS68K task dispatch/creation trap) — this is part of the task initialization logic that creates the RDHC (Read/Display/Handle Channel) task when the system detects a specific task ID (0x13) in the task status table.
0xf0576a [R2 BOTH] This `bne.b loc_F05776` branches if `d5` (a 32-bit argument counter) is non-zero, skipping the error-handling panel command (0x26C) — this is part of the panel command dispatch routine that checks for errors (bit 13 of status) and only issues the abort command if there are no remaining 32-bit arguments to transfer.
0xf04934 [R2 BOTH] This `movea.l #$ff0000, a0` loads the base address of the AP I/F register block into `a0` — this is part of the interrupt service routine (at `loc_F04930`) that handles XP-32 channel interrupts, saving all registers with `movem.l` and then accessing the AP I/F status/control registers to process the interrupt.
0xf055d4 [R2 BOTH] This `cmpa.l #$1ffff, a1` compares the destination address `a1` against the upper bound of the microcode staging buffer (0x1FFFF) — this is part of the S-record data writing routine that enforces the address range check (0x10000 ≤ addr ≤ 0x1FFFF) before writing each 16-bit word to the staging buffer, branching to issue a panel command (0x25A) if the address is out of range.
0xf0517e [R2 BOTH] This `move.w #$400, $218(a5)` writes the value 0x400 to the XLTR status/IRQ register at offset `$218` (which maps to `0xFF0218`), setting bit 10 (the "ready" bit) — this is part of a data transfer routine that initiates a read operation by setting the ready flag, then polls for bit 15 (done) before reading the data word from `(a0)`.
0xf0540c [R2 BOTH] This `adda.l d3, a3` adds the computed index `d3` (derived from the channel number `d4`) to the base address of a dispatch table at `loc_F046E0`, producing a pointer to a channel-specific handler address — this is part of the per-channel command dispatch logic that selects the appropriate handler routine for the XP-32 channel (XP1-XP4) based on the channel number, with the handler addresses stored in a table at `0xF046E0`.
0xf047d6 [R2 BOTH] This `bra.w loc_F047E6` is part of the TCBRDHC task's channel dispatch logic; it skips over the error-handling path when the `move.w #$27a, d0` panel command has been successfully sent via `PanelIOConfigure_25A`, continuing to the normal completion path that sets a status bit in the TCB's channel control word at `$202(a5)`.
0xf04f92 [R2 BOTH] This `bra.b loc_F04F9C` is part of the S-record address validation in `SRecordDataHandler`; it branches to `ChannelConfigDispatch` when the address at `$e7e.l` is outside the valid microcode staging range (0x10000–0x1FFFF), having already sent panel command 0x25A (an error/abort code) to the XP-32.
0xf05832 [R2 BOTH] This `bne.b loc_F0583C` is the "ready" check in the panel command send-and-wait kernel; it branches when bit 14 of the AP I/F command/status register (at `0xFF0000`) is set, indicating the XP-32 is ready to accept the next command, exiting the 1000-tick polling loop.
0xf0469a [R2 BOTH] This `ori.b #$0, d0` at `0xf0469a` is **DATA** — it is part of the ASCII string "USER" (bytes 0x55 0x53 0x45 0x52) that appears in the TCB definition table or related RMS68K marker data, not executable code.
0xf054b0 [R2 BOTH] This `move.l (a0)+, d2` is part of the S-record data handler's checksum/byte-count accumulation logic; it loads the next data word from the S-record buffer into `d2`, then adds it to `d3` to verify the total byte count does not exceed 16 (0x10) before dispatching to `PanelIOConfigure_25A` with error code 0x25B if violated.
0xf07258 [R2 BOTH] Updates VersaBUS XLTR Mode 0 register (FF0200) during hardware configuration.
0xf0863e [R2 BOTH] Sets test parameter to 9 on AP I/F error status (bit 13) during diagnostics.
0xf09998 [R2 BOTH] Executes data pattern test subroutine for bus/memory integrity checks in hardware init.
0xf0630a [R2 BOTH] This `move.w (a7)+, d4` is part of the XP-32 interrupt handling code; it pops a channel index from the stack into `d4`, which is then used to index into a lookup table at `loc_F0668C` to clear the corresponding bit in the IRQ Mask register (`$21a(a4)`), acknowledging the interrupt for that specific XP-32 channel.
0xf091c0 [R2 BOTH] This `movem.l (a7)+, d0-d1/a4-a5` is the return epilogue of the `PanelBusDiagnostic` subroutine; it restores the saved registers from the stack before executing `rts`, completing the diagnostic test that exercises the panel bus by writing channel select values to `$204(a6)` and calling subroutines `loc_F091C6` and `loc_F091FE`.
0xf09808 [R2 BOTH] This instruction writes the 16-bit value in d1 to the address pointed to by a0, which is part of a memory test pattern verification loop (likely in `RAMAddressingTest` or `MemBusProbe`) that checks for stuck-at faults by writing and comparing patterns.
0xf071b4 [R2 BOTH] This pushes a zero longword onto the stack pointed to by a3, part of building an RMS68K `!IOV` (I/O Vector) structure — specifically zeroing the IOV's parameter block fields during `Init_IOV_StoreTag` initialization.
0xf0a0ea [R2 BOTH] This loads the address `$c(a4)` into a4, advancing the pointer to the next field in an RMS68K `!TCB` (Task Control Block) structure during TCB initialization, specifically after storing the task priority byte at offset 5.
0xf091d0 [R2 BOTH] This loads the sentinel value `0xF0F0F0F0` into d7 as an error flag, triggered when a bit test (`btst.l`) on the VERSAmodule control register at `$1(a5)` fails — part of the `PanelBusDiagnostic` self-test that verifies panel bus handshake lines.
0xf05f9c [R2 BOTH] This is an unconditional branch to `loc_F060F6`, the error exit path in the TCBXP4I channel task after a `PanelTimeoutAbortPath` call — it handles the case where the XP-32 channel 4 configuration command (opcode 0x26E) timed out or failed.
0xf085a2 [R2 BOTH] This zeros the longword at offset `$10de(a2)` within an RMS68K `!UST` (User System Table) structure during `Init_UST_StoreTag`, clearing the user-defined data pointer field as part of system table initialization.
0xf0691e [R2 BOTH] This is DATA — the bytes `0x00 0x00 0x53 0x54` are the ASCII string "ST" (part of "STCK" = "STACK") within the `TCBXP3I_CRTCBParams` data block, which defines the stack size and name for the XP-32 channel 3 task control block.
0xf07420 [R2 BOTH] This loads the longword at address `0x106e` (in RAM) into d1 — this is the microcode staging buffer base address or a transfer count parameter, used by the XP-32 channel 1 data port write sequence that follows (writing to `0xFF006E` and `0xFF0068`).
0xf06d0c [R2 BOTH] Writing XP-32 per-channel data port during panel command dispatch.
0xf08f6e [R2 BOTH] Returning from I/O channel diagnostic.
0xf04e26 [R3 BOTH] This instruction tests bit 4 of the global flag at $e87.l, which controls whether the RTOS kernel increments the event counter at $e58.l during channel configuration dispatch — part of the RMS68K task scheduling and event tracking logic within the TCB dispatch loop.
0xf05536 [R3 BOTH] This instruction clears d5 to zero as part of handling the "S1" S-record type (0x5331), preparing for a subsequent subroutine call at loc_F055A2 that processes S1 data records during microcode upload to the XP-32 staging buffer.
0xf05534 [R3 BOTH] Branches to next panel command case in TCBIO1I if opcode 0x5331 not matched.
0xf052f2 [R3 BOTH] This instruction reads a word from the AP I/F data port (a0 points to the command/data interface at 0xFF0000) into d2, retrieving the next data word after a DMA transfer completion signaled by bit 15 of the XLTR status register at $218(a5) — part of the S-record data fetch from the host.
0xf058b0 [R3 BOTH] This is a return from subroutine (rts) that exits the panel error mask clearing routine, which has just written the CONTINUE-TRANSFER opcode (0x8005) to the AP I/F command register at (a0) and cleared the appropriate error mask bit in the XLTR IRQ Mask register at $21a(a4).
0xf04f94 [R3 BOTH] This instruction clears the global flag at $e74.l to zero, indicating successful completion of a microcode address range check (0x10000 to 0x1FFFF) before branching to ChannelConfigDispatch — part of the S-record finalization logic that validates the staging buffer address.
0xf0520e [R3 BOTH] This instruction stores the byte from d2 into the microcode staging buffer at address a1 (which has been validated to be within 0x10000-0x1FFFF), then increments a1 — the core byte-by-byte write operation of the S-record data handler during XP-32 microcode upload.
0xf057a8 [R3 BOTH] This instruction writes the swapped high word of d1 to offset $2 of the per-channel data port at (a1), completing the 32-bit argument transfer for a CONTINUE-TRANSFER (0x8005) panel command — part of the 32-bit argument dispatch in PanelIOConfigure_25A.
0xf047c0 [R3 BOTH] This instruction sets d0 to 0x0D (13 decimal), which is the RMS68K trap #1 function code for "Task Delay" — used to yield the CPU for a short period before retrying the panel command dispatch in the TCBRDHC main loop.
0xf057ee [R3 BOTH] This instruction reads the XLTR IRQ Mask register at $21a(a4) into d0, as part of the error handling path after a panel command timeout (opcode 0x26a abort) — it clears the specific error mask bit indexed by d4 using PanelErrorMaskTable, then writes back to re-enable interrupts.
0xf053b6 [R3 DS] This instruction loads the immediate value `0x48585030` ("HXP0" ASCII) into d1, which is used as a marker or tag for an RMS68K structure (likely a TCB or IOV tag) during the RDHC channel initialization or error handling path.
0xf0505c [R3 DS] This is an unconditional branch to `loc_F0507E`, continuing the flow after selecting between two possible data sources (offset `0x1020` or `0x101e` from a1) based on bit 6 of the status word at `0xe87`, which controls which XP-32 channel's configuration data is being written to the global status register `0xe74`.
0xf04a4e [R3 DS] This is an unconditional branch to `ChannelConfigDispatch`, the central dispatcher that routes channel configuration commands based on the value in d0, after having cleared the global status register `0xe74` and potentially saved the user stack pointer.
0xf04a12 [R3 DS] This `swap` instruction exchanges the upper and lower 16-bit words of d1, which holds the user stack pointer (usp) value, so that the upper word can be written to the global status register `0xe74` as part of a channel configuration dispatch when d0 > 0x44.
0xf048de [R3 DS] This `andi.w #$1f, d0` masks the error status word from `0xe86` to extract the lower 5 bits (the error code), which is then compared against specific values (0x14, 0x13) to determine the error handling path in the TCBRDHC error handler.
0xf051f8 [R3 DS] This instruction reads a 16-bit word from the address pointed to by a0 (the AP I/F data port) into d2, as part of the S-record data handler that validates and copies microcode data into the staging buffer at addresses 0x10000-0x1FFFF.
0xf049ca [R3 DS] This `cmpi.w #$44, d0` compares the channel command value in d0 against 0x44 (68 decimal), determining whether to store the channel's configuration word directly onto the stack or to use the user stack pointer for larger values, as part of the channel configuration dispatch logic.
0xf04604 [R3 DS] This is **DATA** — the bytes `0x0000 0x0000` at address 0xf04604 are part of the TCBLookupTable, which contains 8-byte entries (likely TCB definition pointers or RMS68K structure headers), not executable code.
0xf058b6 [R3 DS] This `swap d1` instruction swaps the halves of d1 (which holds a 32-bit argument) so that the upper 16 bits can be written to offset `$2(a1)` as part of the 32-bit argument transmission protocol for panel commands, following the `0x8005` (CONTINUE-TRANSFER) opcode.
0xf0521e [R3 DS] This `ble.b loc_F05224` checks if the word at `(a1)` is ≤ 0 (i.e., the AP I/F status register indicates ready/not-busy), and if so, proceeds to send panel command `0x25a` via `PanelIOConfigure_25A` to finalize the microcode upload sequence.
0xf0504a [R3 BOTH] Loads a panel command argument from a channel configuration table into the panel command argument register during XP-32 channel setup.
0xf058ea [R3 BOTH] This is the timeout decrement loop in the PanelSendAndWait routine, polling the AP I/F command/status register at (a0) for bit 14 (ready) with a 1000-tick timeout counter in d5.
0xf04f7c [R3 BOTH] This instruction validates that the microcode staging buffer address at $e7e.l is within the valid range 0x10000-0x1FFFF (the upper 64KB RAM region used as the WCS staging buffer), enforcing the SRecordDataHandler's address constraint.
0xf049e6 [R3 BOTH] Preserves the user stack pointer in data register during a channel configuration context switch.
0xf04d66 [R3 BOTH] This conditional branch checks bit 6 of $e87.l to determine which channel configuration path to take, part of the per-channel command dispatch that extracts and formats address parameters from $e58.l for the XP-32 interface.
0xf05166 [R3 BOTH] Masks an ASCII hex character value for numeric conversion in panel command data processing.
0xf056a4 [R3 BOTH] This instruction writes the modified Mode 1 register value (with bit 12 set and bit 14 cleared) to the XLTR register at $FF0202, configuring the VersaBUS translator for panel command transmission in PanelIOConfigure_25A.
0xf04a78 [R3 BOTH] This instruction shifts the channel index (from $e86.l masked to 0-15) left by 2 bits to create a word-aligned offset into the dispatch table at loc_F05102, implementing the per-channel command jump table.
0xf05468 [R3 BOTH] This instruction calls the PanelSendAndWait kernel routine (at F056BA) to transmit a panel command to the XP-32, using parameters from the TCB or control block pointed to by a6 (containing command code, arguments, and timeout values).
0xf058fa [R3 BOTH] This is the loop-back branch in the timeout polling loop of PanelSendAndWait, continuing to decrement d5 and poll the AP I/F status register until either bit 14 (ready) is set or the 1000-tick timeout expires.
0xf04796 [R3 BOTH] This `trap #$1` at 0xf04796 is an RMS68K RTOS call to create or validate a task control block (TCB) for the RDHC channel; if it fails (beq not taken), the code sends panel command 0x279 via `PanelIOConfigure_25A` to abort or reconfigure the XP-32 channel.
0xf0525c [R3 BOTH] At 0xf0525c in `SRecordFinalize`, `move.w #$8, d5` sets the byte count for an S3 record (8 data bytes) before finalizing the microcode upload to the XP-32 staging buffer at 0x10000–0x1FFFF.
0xf04b42 [R3 BOTH] At 0xf04b42, `move.w #$0, $218(a5)` clears the status/IRQ register at XLTR offset 0x218 (bit 15 = ready/done), acknowledging completion of a DMA or panel command before proceeding to the next transfer in the XP-32 channel dispatch loop.
0xf058b4 [R3 BOTH] At 0xf058b4, `move.w d1, (a1)` writes the low 16 bits of a 32-bit argument to the per-channel data port (a1 points to FF0244/FF0246/FF0250/FF0252), as part of the 0x8005 CONTINUE-TRANSFER command sequence for sending 32-bit panel arguments to the XP-32.
0xf05756 [R3 BOTH] At 0xf05756, `cmpi.l #$0, d5` checks if the 1000-tick timeout counter (d5) has expired while polling bit 14 (ready) of the AP I/F command/status register at 0xFF0000; if zero, it triggers an abort via panel command 0x26C.
0xf0628e [R3 BOTH] At 0xf0628e, `jsr PanelTimeoutAbortPath.l` is called when the timeout counter expires during a panel command wait loop (polling bit 14/13 at 0xFF0000), invoking the abort handler that sends command 0x26C to reset the XP-32 channel.
0xf09aaa [R3 BOTH] At 0xf09aaa, `subq.l #$1, d5` decrements a delay loop counter (initialized to 0x493E0 = 300,000 iterations) in the RAM addressing test, providing a timing delay before comparing memory patterns to detect addressing faults.
0xf09aa4 [R3 BOTH] Initialize delay counter for board status polling.
0xf07b5c [R3 BOTH] At 0xf07b5c, `ble.b loc_F07B6A` checks if the channel number (d0) is within valid range (1 to value at 0x105E); if out of range, it sends panel command 0x264 via `loc_F07CC0` to abort the XP-32 channel configuration.
0xf095c4 [R3 BOTH] At 0xf095c4, `bne.b loc_F095E8` compares a bitmask pattern (starting at 0x10) against the XLTR Mode 2 register values at offsets 0x210–0x22E (a6-based); a mismatch indicates the VERSAmodule XLTR is not in the expected configuration, causing the hardware init to skip or fail.
0xf0648c [R3 BOTH] Clearing the XLTR status/IRQ register at $218(a4) after polling bit 15 (ready) in the AP I/F command/status handshake loop, part of the panel command send-and-wait kernel (F056BA) that dispatches XP-32 panel commands.
0xf09ad4 [R3 BOTH] Return from subroutine (rts) after completing the ROM checksum XOR calculation loop (ROMChecksum_XOR at F098EE), which XORs ROM contents into d7 for verification.
0xf09078 [R3 BOTH] Loading the address of loc_F0911E into a1 for installation as an exception vector at $150, part of PTMInit (F09176) or the PanelBusDiagnostic (F0919C) setup for MC6840 PTM timer interrupt handling.
0xf06dd8 [R3 BOTH] Testing bit 13 (error) of the AP I/F command/status register (d4 from $200(a4)), part of the panel command dispatch error handling that issues abort command 0x26B and clears the corresponding bit in the IRQ mask register at $21A(a4).
0xf059be [R3 BOTH] Comparing d0 against immediate values 0x08 and 0x18 in the SRecordDataHandler (F051A2), checking if the S-record address field indicates a 2-byte or 3-byte address type for microcode staging buffer validation (0x10000 ≤ addr ≤ 0x1FFFF).
0xf08f2a [R3 BOTH] Loading the current exception vector for vector $8 (bus error) into a2, part of the IOChannelDiagnostic (F08F72) or MemBusProbe (F08EAC) that installs a temporary handler to catch bus faults during memory probing.
0xf078d0 [R3 BOTH] Copy word from d6 to XLTR data register (FF0214) for XP32 command preparation.
0xf07476 [R3 BOTH] Conditional branch (beq) after a trap #1 (RMS68K system call) that creates or opens a named resource, checking if the operation succeeded; on failure, it issues panel command 0x271 via loc_F07CC0 as part of XP-32 channel configuration.
0xf09ca6 [R3 BOTH] Branch-if-equal (beq) after comparing the 4-byte marker at (a2)+ with the constant $21564354 ("!VCT"), part of VCTScanSetup (F09C96) that scans memory for the Vector/Config Table signature to locate the RMS68K configuration data.
0xf0891a [R3 BOTH] Return from exception (rte) after incrementing a watchdog or timeout counter (either $1F800.l or $400.w depending on stack pointer range), part of a fault handler or diagnostic routine that tracks system stability during hardware initialization.
0xf04a3e [R4 DS] This instruction (`move usp, a1`) is part of the channel configuration dispatch logic, where the User Stack Pointer is manipulated to store/restore per-channel context during XP-32 command processing, specifically handling the case when d0 > 0x44 in the channel dispatch table at `ChannelConfigDispatch`.
0xf05198 [R4 DS] This `subq.w #$1, d4` decrements the byte counter during the S-record data handler's microcode upload loop, controlling how many bytes remain to be written to the XP-32 microcode staging buffer (0x10000-0x1FFFF) before finalizing the transfer.
0xf056b0 [R4 DS] This `move.w d1, $200(a0)` writes to the XLTR Mode 0 register (0xFF0200) with bit 10 cleared, completing the configuration sequence in `PanelIOConfigure_25A` that sets up the VersaBUS XLTR for panel command transmission to the XP-32 array processor.
0xf056f2 [R4 DS] This `bne.b loc_F056FE` branches when the timeout counter (d5) has expired during the `PanelSendAndWait` polling loop, leading to either a successful completion check or an abort command (0x269) if the AP I/F status indicates an error (bit 13 set).
0xf049be [R4 DS] This `btst.b #$5, $e87.l` tests bit 5 of the global status word at 0xE87, which selects between two different channel configuration paths in the dispatch logic, determining whether to use the User Stack Pointer or direct register manipulation for channel context handling.
0xf04dec [R4 DS] This `move.l $e58.l, d1` loads a 32-bit value from the global variable at 0xE58, which contains the XP-32 microcode address being computed for DMA transfer, used to calculate the target address in the staging buffer for the next microcode word upload.
0xf0582a [R4 DS] This `subq.l #$1, d5` decrements the 1000-tick timeout counter in the `PanelSendAndWait` polling loop, waiting for the AP I/F ready bit (bit 14) to be set after issuing a CONTINUE-TRANSFER (0x8005) command during 32-bit argument transmission.
0xf05492 [R4 DS] This `bclr.b #$1, $10a1(a0)` clears bit 1 in a per-channel status/control structure at offset 0x10A1, part of the RMS68K I/O Vector (IOV) initialization or cleanup routine, likely marking a channel as inactive or clearing a pending operation flag.
0xf0589c [R4 DS] This `clr.l d5` zeroes the error mask index register before loading a byte from `PanelErrorMaskTable` (at 0xF05C4C), which maps channel error codes to specific bit positions in the IRQ Mask register (0xFF021A) for clearing XP-32 error flags after command completion.
0xf057f2 [R4 DS] This `move.w (a7)+, d4` pops the saved channel number from the stack after an error abort command (0x26A) has been sent via `PanelIOConfigure_25A`, restoring the channel context for subsequent error mask manipulation and status register update.
0xf05660 [R4 BOTH] This instruction sets up the stack pointer for a TRAP #1 call (RMS68K system call) to perform a kernel operation, likely related to task management or I/O dispatch in the RTOS kernel.
0xf058fa [R4 BOTH] This branch loops back to poll the AP I/F command/status register at 0xFF0000, waiting for bit 14 (ready) to be set, with a timeout of 1000 ticks — part of the panel command send-and-wait kernel.
0xf0551a [R4 BOTH] This increments a counter (d0) while parsing S-record data, tracking the number of 16-bit words processed during microcode upload to the staging buffer (0x10000-0x1FFFF).
0xf04e88 [R4 BOTH] This adds 8 to d1 to adjust a channel configuration offset, based on a test of bit 6 in $e87.l — part of the per-channel command dispatch logic for XP-32 channels.
0xf04630 [R4 GLM] Decrements reference count in task control block during RDHC error handling.
0xf048fa [R4 BOTH] This masks d0 with 0x1F to extract a 5-bit error code from the AP I/F status register ($e86.l), then compares it to 0x13 (19 decimal) to determine the error handling path in the RDHC channel task.
0xf049fa [R4 BOTH] Dispatches to channel-specific handler after setting task context (RDHC/XP32).
0xf0504a [R4 BOTH] Loads channel-specific config value from table into $e74 register during XP-32 setup.
0xf04992 [R4 BOTH] This compares d0 to 0x13 (19 decimal) to check for a specific command code, branching to a handler that clears the command argument ($e74.l) and dispatches to ChannelConfigDispatch — part of the command decoding in the RDHC task.
0xf0554e [R4 BOTH] This sets d5 to 0x10 (16 decimal) as a parameter for a subsequent subroutine call (loc_F055A2), which handles S-record data parsing for microcode upload — specifically for record types "S2" or "S3" (0x5332/0x5333).
0xf046c8 [R4 GLM] Decrement stack pointer in TCB for microcode upload task initialization.
0xf0565e [R4 BOTH] Push microcode data length or buffer address onto system call stack for microcode staging.
0xf05174 [R4 DS] This is a branch instruction in the hex-to-binary conversion routine within SRecordDataHandler, specifically handling ASCII-to-hex conversion of S-record data bytes (branching after subtracting 0x37 for uppercase hex letters A-F).
0xf04d6a [R4 BOTH] This loads a 32-bit value from global variable $e58.l into d1, which is then used to extract a channel configuration field (shifted right by 20 bits) and a memory address for XP-32 microcode staging — part of the channel configuration dispatch logic.
0xf053ec [R4 DS] This shifts d3 left by 5 bits (multiply by 32) to compute an offset into a data structure, then adds base address 0xFF000E to form a pointer — likely calculating the address of a per-channel command argument register in the AP I/F register map.
0xf04ee8 [R4 BOTH] Compare channel number (from register) to zero to determine if channel is valid for panel command dispatch.
0xf0525c [R4 BOTH] This sets d5 to 8 (0x08) when d4 equals 3, indicating the S-record type 3 (32-bit address format) in SRecordFinalize — this determines the address field width for finalizing an S-record upload to the XP-32 microcode staging buffer.
0xf05298 [R4 BOTH] This clears a1 (sets to zero) as part of the SRecordFinalize routine, preparing for a loop that polls the XLTR status register (FF0218 bit 15) waiting for the XP-32 panel bus to become ready before writing final data.
0xf05338 [R4 BOTH] Send panel command 0x259 (possibly to configure or start microcode upload) when staging buffer is empty.
0xf056fe [R4 BOTH] This tests bit 13 of d4 (the AP I/F status register value), checking for an error condition during the panel command send-and-wait kernel — if set, it will issue abort command 0x269 via PanelIOConfigure_25A to the XP-32 array processor.
0xf048d2 [R4 BOTH] This instruction stores a modified value to the VERSAmodule control register at offset $200(a5), where bit 10 has been cleared to configure the board status/control register for a specific mode, likely related to panel bus or interrupt control during TCBRDHC error handling.
0xf0522e [R4 BOTH] This is a return from subroutine (RTS) after calling PanelIOConfigure_25A with opcode 0x25A, which is a Group A SUBRC instruction (ACC ← I − RAM[R24..R31] − ¬c) used to send a panel command to the XP-32, likely as part of the SRecordFinalize sequence to finalize microcode upload.
0xf04948 [R4 BOTH] This instruction sets bit 10 in the AP I/F command/status register at $200(a0) (address 0xFF0200), which is the Mode 0 register of the XLTR control block, used to configure the VersaBUS translator for channel selection or DMA handshaking during XP-32 command dispatch.
0xf0582e [R4 BOTH] This instruction tests bit 14 (ready) of the AP I/F command/status register at (a0)=0xFF0000, polling for the XP-32 to signal completion of a CONTINUE-TRANSFER (opcode 0x8005) operation, with a 1000-tick timeout, as part of the panel command send-and-wait kernel.
0xf057a8 [R4 BOTH] This instruction writes the swapped high word of d1 to the data register high half / Cmd at $2(a1) (address 0xFF0216), which is part of the XLTR register map used to send a 32-bit argument to the XP-32 via the CONTINUE-TRANSFER protocol, following the initial opcode 0x8005.
0xf07dae [R4 BOTH] This instruction decrements address register a1 by 2 (subq.l #$2, a1), used in a loop that copies data from a table at loc_F07D3E to the stack area at (a5), likely building a parameter block for a TRAP #1 call (RMS68K system call) during RTOS kernel initialization or task creation.
0xf07cdc [R4 BOTH] This instruction writes the modified d1 value to the Mode 1 register at $202(a0) (address 0xFF0202), where bit 12 is set and bit 14 is cleared, configuring the XLTR for a specific channel operation, likely as part of the per-channel command dispatch to the XP-32 EXEC/ARITH cards.
0xf09146 [R4 BOTH] This instruction loads the immediate value 0xF0F0F0F0 into d7, which is a known "dead" or "poison" pattern used in the PTMInit or diagnostic routines to detect uninitialized memory or bus errors during the MC6840 PTM (Programmable Timer Module) initialization test.
0xf06ad0 [R4 BOTH] This instruction writes the value 0x8000 to the per-channel data port at a0=0xFF008E (XP3 channel data port), which is part of the XP-32 communication primitives to send a command or data word to channel 3, likely as part of a microcode upload or status query sequence.
0xf09dd8 [R4 BOTH] This instruction masks d0 with 0x70 (bits 4-6), extracting a 3-bit field from a byte that was loaded from a table or data structure, used in the Init_GST_StoreTag or similar RMS68K initialization routine to decode a tag type or record subtype when building the Global System Table entries.
0xf07fce [R4 BOTH] This instruction tests bit 13 of d4, which is the error status bit from the AP I/F command/status register at 0xFF0000; it is part of the panel command send-and-wait kernel that checks for errors after issuing a panel command (0x26b in this path) to the XP-32.
0xf05b36 [R4 BOTH] This instruction reads a 16-bit word from offset 2 of the data structure pointed to by a1, which is part of the SRecordDataHandler's microcode staging buffer copy logic; it handles the case where d0 is zero, copying a second word from the S-record data to the destination buffer during the 32-bit data transfer to the XP-32 microcode staging area (0x10000-0x1FFFF).
0xf08720 [R4 BOTH] This instruction writes 0x0050 to the VERSAmodule control register at 0x01FFF0, which is part of MainInit's hardware initialization phase 1; it configures the VERSAmodule bus control logic (likely setting bus timeout or arbitration parameters) before proceeding to check board status and initialize the XP-32 interface.
0xf08658 [R4 BOTH] Configuring DMA control register for XP-32 microcode upload to staging buffer (0x010064)
0xf091aa [R4 BOTH] This instruction writes d6 (which is being incremented through values 0, 1, 2) to the Channel Select register at 0xFF0204 (a6 points to 0xFF0200), which is part of the PanelBusDiagnostic selftest; it sequentially selects each of the three panel bus channels (0, 1, 2) to test communication with the XP-32 boards via the XLTR interface.
0xf08d3e [R4 BOTH] Computing XOR checksum of ROM (0xF00000) during boot self-test integrity verification
0xf06420 [R4 BOTH] This instruction reads the AP I/F command/status register at 0xFF0000 (pointed to by a0) into d4, which is part of the panel command send-and-wait kernel's polling loop; it checks bit 14 (ready) and bit 13 (error) with a 1000-tick timeout (d5) to determine if the XP-32 has completed the requested operation (e.g., after issuing opcode 0x8004 REQUEST-TRANSFER).
0xf08ace [R4 BOTH] This instruction moves the complemented address (d1, which is NOT of d0) into a5, which is part of the RAMAddressingTest function; it sets up a5 as the "bad" address pattern (all bits inverted) while a6 holds the original address, then performs a series of address bus tests by writing and reading back patterns to verify all address lines are properly connected in the 64KB RAM at 0x000000-0x00FFFF.
0xf09ed4 [R4 BOTH] This instruction writes the value 1 to offset 8 of the UST (User System Table) structure being initialized at (a0), which is part of the Init_UST_StoreTag function in the RMS68K RTOS kernel initialization; it sets the UST's version or type field to 1, following the storage of the "!UST" marker tag at offset 0, as part of building the Global System Table for task management.
0xf09b74 [R4 BOTH] This instruction is a conditional branch (beq.b) that checks if the value written to memory at (a0) matches the expected pattern in d1, which is part of the RAMAddressingTest's data bus test; it verifies that the 16-bit data pattern written to each RAM location can be read back correctly, and if not, sets d7 to a failure sentinel (0xF0F0F0F0) and calls the error handler.
0xf056ba [R5 BOTH] This is the entry point of `PanelSendAndWait`, which writes the busy flag `0x4F` to the panel status register (pointed to by `a3`), then initiates a `REQUEST-TRANSFER` (opcode `0x8004`) to the AP I/F command register at `0xFF0000` and begins polling for completion with a 1000-tick timeout — the core panel command dispatch mechanism for XP-32 microcode upload and channel configuration.
0xf04c00 [R5 BOTH] This code compares register `d1` against ASCII values `0x5338` (`"S8"`) and `0x5339` (`"S9"`) to detect S-record termination records; if matched, it calls `SRecordFinalize` to complete the microcode staging buffer upload, then checks a flag at `$e74` to decide whether to branch to `ChannelConfigDispatch` — part of the S-record parsing and microcode upload pipeline for the XP-32 WCS bank.
0xf050de [R5 BOTH] This branch at `f050de` jumps to `loc_F050EC` after clearing bit 7 of the channel configuration word at `$202(a0)` when the channel select register (`$204(a0)`) is zero, implementing a conditional clear of a "busy" or "active" flag for the XP-32 channel — part of the per-channel configuration logic in the microcode upload dispatch.
0xf04844 [R5 BOTH] This instruction calls `PanelIOConfigure_25A` with opcode `0x25C` when the value at `$e60.l` is out of range (≤0 or >`$105e`), serving as an error-handling path that sends a panel command to abort or reset the XP-32 channel configuration — part of the channel parameter validation in the microcode upload state machine.
0xf04d1c [R5 BOTH] This unconditional branch to `ChannelConfigDispatch` at `f04d1c` occurs after storing the channel select value from `$204(a0)` into either `$e58.l` or `$e5a.l` depending on a prior comparison, completing the configuration of a channel parameter and returning to the main dispatch loop for the next microcode upload operation.
0xf04c4c [R5 BOTH] This branch at `f04c4c` jumps to `loc_F04C6E` when the comparison `cmp.l $e64.l, d0` indicates the value is not less than the stored limit, continuing a loop that copies microcode data from the staging buffer to the AP I/F data ports — part of the XP-32 microcode transfer routine that writes 16-bit words to the per-channel data ports at `0xFF0008+`.
0xf05176 [R5 DS] This instruction subtracts `0x30` from register `d2` to convert an ASCII hex digit character (in the range `'0'`-`'9'`) to its numeric value (0-9), as part of the hex-to-binary conversion routine used by `SRecordDataHandler` when parsing S-record data for microcode upload to the staging buffer at `0x10000-0x1FFFF`.
0xf049e4 [R5 BOTH] This instruction `move usp, a2` reads the User Stack Pointer into address register `a2`, then manipulates it by swapping and inserting the channel select value from `d1` — this is part of a special handler that stores the channel number into the user stack pointer for later retrieval, likely a context-switch or task-control mechanism in the RMS68K RTOS for the XP-32 channel dispatch tasks.
0xf0581e [R5 BOTH] This conditional branch `bne.b loc_F0588A` checks bit 11 of the AP I/F status register (read into `d5` from `(a0)`); if set, it skips the `CONTINUE-TRANSFER` (opcode `0x8005`) command — part of the 32-bit argument follow-up protocol in `PanelSendAndWait` where the second word of a two-word panel command is only sent if the first word's status indicates readiness.
0xf054b2 [R5 BOTH] This instruction `add.l d2, d3` accumulates a running total of microcode block sizes during S-record parsing; the result in `d3` is compared against `0x10` (16) to enforce a maximum block size limit, and if exceeded, triggers a panel command `0x25B` (likely an abort or error code) — part of the S-record data validation in the microcode upload handler.
0xf051d4 [R5 BOTH] This instruction (`subq.b #$8, d5`) decrements the bit-position counter `d5` by 8 during S-record data parsing in `SRecordDataHandler`, advancing to the next byte of the packed hex data for microcode staging buffer writes (address range 0x10000–0x1FFFF).
0xf05204 [R5 BOTH] This instruction (`blt.b loc_F05212`) checks if the staging buffer address in `a1` is below 0x10000, enforcing the valid microcode upload range (0x10000–0x1FFFF) in `SRecordDataHandler`; out-of-range addresses trigger error handling via the AP I/F at 0xFF0000.
0xf04b66 [R5 BOTH] This instruction (`addq.l #$1, d0`) increments the byte counter `d0` after reading a 16-bit word from the AP I/F data port during a panel command response read, part of the per-channel command dispatch loop in the RDHC task.
0xf04694 [R5 GLM] TCB initialization (setting task state flag to 0x50).
0xf048fa [R5 DS] This instruction (`andi.w #$1f, d0`) masks the error code from global variable `0xe86` to 5 bits, checking for error code 0x13 (decimal 19) in `TCBRDHC_ErrorPath`, which triggers a specific error handler via trap #1 with a message at `loc_F0469E`.
0xf04fe2 [R5 BOTH] This instruction (`lsl.w #$1, d1`) left-shifts the channel index from `$e7a` by 1 to convert it to a word offset into the channel configuration table at `$1064(a1)`, used in `ChannelConfigDispatch` to select per-channel data ports (XP1–XP4).
0xf0584e [R5 BOTH] This instruction (`jsr PanelIOConfigure_25A.l`) sends panel command 0x26C via the `PanelIOConfigure_25A` function, which is a Group B SUBRC instruction (ACC ← RAM[N] − D − ¬c) used to configure a specific XP-32 channel parameter during the channel initialization sequence.
0xf05674 [R5 BOTH] This instruction (`movea.l (a7)+, a0`) pops a return address from the stack into `a0` at the end of a trap handler (trap #$29/#$2A), restoring the caller's address register after a system call for panel command dispatch or error handling.
0xf04c8e [R5 BOTH] This instruction (`cmpi.w #$0, d3`) checks if the channel parameter `d3` (loaded from `$e60`) is non-positive, triggering panel command 0x25C (a Group A SUBRC instruction) via `PanelIOConfigure_25A` if invalid, as part of channel configuration validation in `ChannelConfigDispatch`.
0xf046f0 [R5 BOTH] This instruction (`moveq #$1, d0`) sets the task priority/type to 1 for the `TCBRDHC_Entry` task entry point, then calls trap #1 with a pointer to the TCB definition at `loc_F046B0` to initialize the RDHC (Read/Display/Handle Channel) master dispatch task.
0xf04b80 [R5 BOTH] Increments d0 during XP-32 microcode S-record parsing in SRecordDataHandler, tracking byte offset in the 64KB staging buffer.
0xf055aa [R5 BOTH] This `addq.l #$1, d0` increments the running byte counter during the checksum/address accumulation loop in the S-record finalization routine (SRecordFinalize), where d0 tracks the total number of bytes processed for the final CRC verification.
0xf04c3e [R5 BOTH] This `bra.w ChannelConfigDispatch` branches to the channel configuration dispatch routine after sending panel command 0x25F (a SUBRC instruction to the Am29116) via PanelIOConfigure_25A, which triggers the XP-32 EXEC card to perform a specific RAM-based operation for channel setup.
0xf05662 [R5 BOTH] This `moveq #$29, d0` loads trap vector number 0x29 (decimal 41) for a TRAP #1 call, invoking the RMS68K RTOS kernel's "send message" or "signal semaphore" service to post a completion notification to a waiting task from the PanelIOConfigure_25A function.
0xf04b8e [R5 BOTH] This `bne.b loc_F04B9A` branches when the S-record type code in d1 is not "S0" (0x5330), directing the parser to check for "S1" records (0x5331) which contain the actual microcode data to be written to the XP-32 staging buffer.
0xf0518e [R5 BOTH] This `move.w #$0, $218(a5)` clears the XLTR status/IRQ register at 0xFF0218 (via a5 pointing to 0xFF0200), acknowledging the completion of a DMA transfer or panel command to the XP-32 before reading the next data word from the S-record stream.
0xf0526e [R5 BOTH] This `cmpi.w #$5, d4` checks if the S-record byte count field (d4) equals 5, indicating an S2 record type with 3-byte addresses, which determines the shift amount (d5=0x18) for extracting the 24-bit target address in the microcode staging buffer.
0xf04c9c [R5 BOTH] This `move.w #$25c, d0` loads panel command code 0x25C (a SUBRC instruction targeting the Am29116's RAM[N]−D−¬c operation) to be sent via PanelIOConfigure_25A, initiating a channel configuration or diagnostic operation on the XP-32 EXEC card.
0xf051dc [R5 BOTH] This `adda.l #$10000, a1` adds 0x10000 to the destination address pointer a1, wrapping or advancing to the next 64KB bank boundary within the microcode staging buffer (0x10000-0x1FFFF) during S-record data processing in SRecordDataHandler.
0xf0582c [R5 BOTH] This `move.w (a0), d4` reads the AP I/F command/status register at 0xFF0000 (pointed to by a0) to poll bit 14 (ready) during the CONTINUE-TRANSFER (0x8005) handshake, implementing the timeout loop for XP-32 panel command completion in the send-and-wait kernel.
0xf04b54 [R5 BOTH] Polls the XLTR status register at $218(a5) waiting for bit 15 (ready/done) to become set, part of the XP-32 panel command send-and-wait kernel that waits for the AP I/F to complete a transfer after issuing opcode 0x8004.
0xf04bb4 [R5 BOTH] Branches to ChannelConfigDispatch after an S-record data handler call completes successfully (when $e74 is nonzero), indicating the microcode staging buffer upload for channel type "S1" (0x5331) has finished and channel configuration must proceed.
0xf051d0 [R5 BOTH] Accumulates a 16-bit word into the microcode staging buffer address (a1) by left-shifting the data by d5 bits and adding to the pointer, part of the SRecordDataHandler that builds the 64KB WCS bank image from S-record data.
0xf0484e [R5 BOTH] Selects XP channel 4 task (TCBXP4I) during per-channel initialization.
0xf054d4 [R5 BOTH] Exchanges address registers a1 and a0 when d1 is nonzero, part of a memory copy routine that transfers data between the microcode staging buffer and the XP-32 channel data ports during microcode upload.
0xf06fe4 [R5 BOTH] Loads the address of a lookup table at F070A4 into a5, then uses d4 as an index to extract a bit position from that table to clear a specific interrupt mask bit in the XLTR IRQ Mask register at $21a(a4), part of interrupt handling for XP-32 channel operations.
0xf08f1a [R5 BOTH] Executes an RTE (Return from Exception) instruction, which is part of an exception handler that restores the stack pointer and returns from a bus error or address error trap during the memory bus probe or diagnostic tests.
0xf08622 [R5 BOTH] Tests bit 11 of a word value in d5, which is part of a routine that examines the board status/control register bits to determine the type of VERSAmodule board revision or configuration, branching based on whether bit 11 indicates a specific hardware variant.
0xf08a9e [R5 BOTH] Sets d6 to 0xFF as a test pattern, then compares it against 0xFFFFFFFF to check for a specific hardware condition, part of the board initialization sequence that probes the AP I/F at $FF0000 and verifies the board status register returns expected values.
0xf062ae [R5 BOTH] Clears d5 to zero before using it as an index into a lookup table at F0668C to clear a specific interrupt bit in the XLTR IRQ Mask register, part of the per-channel command dispatch cleanup that acknowledges completion of a panel command and re-enables interrupts.
0xf09724 [R5 BOTH] Polling board status during VersaBUS XLTR configuration loop
0xf096b8 [R5 BOTH] This is a utility subroutine that clears a word at address `(a1)`; it is used to reset a status/control word (likely the AP I/F command/status register or a per-channel data port) before polling, as part of the panel command dispatch or microcode upload handshake.
0xf06474 [R5 BOTH] This is an unconditional branch to `loc_F06502` at 0xf06474, which likely continues a loop in the SRecordDataHandler or a microcode staging routine; it bypasses a wait-for-ready poll on the XLTR status register at `$218(a4)` when a condition is met (e.g., after setting a counter/config value).
0xf08a3a [R5 BOTH] This is a conditional branch (`bne.b loc_F08A46`) at 0xf08a3a that checks if both bits 4 and 5 of a word from `(a4)` are set; this is part of a board status/control register test (likely the PAL-decoded register at 0xF70018-0xF7001A), determining whether to jump to an error handler or continue hardware initialization.
0xf0938c [R5 BOTH] This instruction at 0xf0938c loads `0xFF` into `d3` as a timeout counter for a `dbne` (decrement-and-branch-until-not-equal) loop at 0xf09390; this is a delay loop used in the panel bus diagnostic or PTM initialization, waiting for a status bit to change in the VERSAmodule control register or board status register.
0xf076ac [R5 BOTH] This is a `btst.b #$d, d4` at 0xf076ac that checks bit 13 (error status) of the AP I/F command/status register; it is part of the panel command send-and-wait kernel, where if the error bit is set, it issues an abort command (0x26A) and clears the corresponding bit in the IRQ mask register at `$21a(a4)`.
0xf09266 [R5 BOTH] Clearing status register before VersaBUS XLTR channel polling loop
0xf044a0 [R5 BOTH] This is a `rts` (return from subroutine) at 0xf044a0, which is the end of a short subroutine that restores registers `a3-a6` from the stack; it is likely a utility function called from the RTOS kernel or a task dispatcher (e.g., after a `trap #$0` system call for task scheduling or I/O).
0xf068d4 [R5 BOTH] This `move.w d0, $204(a0)` at 0xf068d4 writes a channel number or command argument to the Channel Select register at 0xFF0204 (the XLTR register block); it is part of the `PanelIOConfigure_25A` routine, selecting a specific XP-32 channel before sending a panel command via the AP I/F.
0xf0773c [R5 BOTH] This `move.l #$3e8, d5` at 0xf0773c loads the value 1000 (decimal) into `d5` as a timeout counter; it is part of the panel command send-and-wait kernel's polling loop, where `d5` is decremented while waiting for bit 14 (ready) of the AP I/F command/status register at `(a0)` to be set, implementing the 1000-tick timeout.
