#!/usr/bin/env python3
"""Build fps3k.asm - the single consolidated FPS-3000 disassembly.

Input : fps3k_custom_annotated.asm  (frozen; its own generator reads a
        /tmp file and cannot be rerun - see CLAUDE.md)
Output: fps3k.asm

Supersedes fps3k_clean.asm.  Symbol tables here are corrected against the
2026-07-29 findings; the old cleaner's names for the AP I/F channel ports
and several $E5x/$E6x globals were wrong.
"""
import re, datetime

IN  = 'fps3k_custom_annotated.asm'
OUT = 'fps3k.asm'

# ---- AP I/F + XLTR, corrected -------------------------------------------
# The channel window is NOT write/readA/status/readB.  +$08 and +$0A are the
# high and low halves of one 32-bit data register; +$0E is a command/trigger.
IO = {
    0x0000:'APIF_CMD_STATUS',     # $8004/$8005 out; reads <=0 = END OF STREAM
    0x0004:'APIF_READY',          # bit 0 = port ready, polled before transfers
    0x0008:'APIF_BULK_DATA',      # bidirectional: S-record ASCII / binary in / binary out
    0x000E:'APIF_PANEL_CMD',
    0x0010:'APIF_CMD_ARG_HI',
    0x0044:'APIF_CH1_WRITE', 0x0048:'APIF_CH1_DATA_HI', 0x004A:'APIF_CH1_DATA_LO', 0x004E:'APIF_CH1_CMD',
    0x0064:'APIF_CH2_WRITE', 0x0068:'APIF_CH2_DATA_HI', 0x006A:'APIF_CH2_DATA_LO', 0x006E:'APIF_CH2_CMD',
    0x0084:'APIF_CH3_WRITE', 0x0088:'APIF_CH3_DATA_HI', 0x008A:'APIF_CH3_DATA_LO', 0x008E:'APIF_CH3_CMD',
    0x00A4:'APIF_CH4_WRITE', 0x00A8:'APIF_CH4_DATA_HI', 0x00AA:'APIF_CH4_DATA_LO', 0x00AE:'APIF_CH4_CMD',
    0x0200:'XLTR_MODE0', 0x0202:'XLTR_MODE1', 0x0204:'XLTR_CHANNEL_SELECT',
    0x020C:'XLTR_COUNTER', 0x0210:'XLTR_MODE2_PAGE', 0x0214:'XLTR_DATA_LO',
    0x0216:'XLTR_DATA_HI', 0x0218:'XLTR_STATUS_IRQ', 0x021A:'XLTR_IRQ_MASK',
    # $FF0230-$FF025F are BIM registers, not "channel config"
    0x0230:'BIM0_CR0', 0x0232:'BIM0_CR1', 0x0234:'BIM0_CR2', 0x0236:'BIM0_CR3',
    0x0238:'BIM0_VR0', 0x023A:'BIM0_VR1', 0x023C:'BIM0_VR2', 0x023E:'BIM0_VR3',
    0x0240:'BIM1_CR0', 0x0242:'BIM1_CR1', 0x0244:'BIM1_CR2_XP1', 0x0246:'BIM1_CR3_XP2',
    0x0248:'BIM1_VR0', 0x024A:'BIM1_VR1', 0x024C:'BIM1_VR2_XP1', 0x024E:'BIM1_VR3_XP2',
    0x0250:'BIM2_CR0_XP3', 0x0252:'BIM2_CR1_XP4', 0x0254:'BIM2_CR2_IO1', 0x0256:'BIM2_CR3',
    0x0258:'BIM2_VR0_XP3', 0x025A:'BIM2_VR1_XP4', 0x025C:'BIM2_VR2_IO1', 0x025E:'BIM2_VR3',
}

# ---- SBC RAM globals, corrected -----------------------------------------
RAM = {
    0x000400:'g_sched_save_d0', 0x000800:'g_ctx_save',
    0x000C00:'g_tcb_chain_ptr', 0x000C08:'g_saved_sp',
    0x000C36:'g_apif_base',     0x000C3A:'g_apif_ptr', 0x000C66:'g_current_tcb',
    0x000E58:'g_xfer_addr_hi',  0x000E5A:'g_xfer_addr_lo',   # 32-bit destination
    0x000E5C:'g_opcode_latch',  0x000E5E:'g_opcode_lo',      # $28 = bulk transfer
    0x000E60:'g_xp_channel',    0x000E62:'g_xp_channel_lo',  # XP channel 1-4
    0x000E64:'g_xfer_count_hi', 0x000E66:'g_xfer_count_lo',  # 32-bit word count
    0x000E68:'g_data_param_hi', 0x000E6A:'g_data_param_lo',  # -> d3 of PanelSendAndWait
    0x000E6E:'g_last_panel_arg',
    0x000E70:'g_chassis_data_hi',0x000E72:'g_chassis_data_lo',# code $3 read/write data
    0x000E74:'g_result',        # returned to the chassis via CHANNEL_SELECT
    0x000E7A:'g_slot_index',    # codes $A/$C, range-checked 0..$C
    0x000E86:'g_response_word', 0x000E87:'g_response_byte',  # b7 dispatcher, b6/b5 modifiers
    0x00101E:'g_regfile',       # 16 longwords, $101E-$105D
    0x00105E:'g_ac_count',      # installed-AC count, chassis-supplied
    0x001062:'g_task_channel',  # each XP task writes its own number
    0x001064:'g_channel_mask',  # shared bitmask, all four tasks and/or into it
    0x001066:'g_ch_block',      # 4 x 3 words, stride 6
    0x001080:'g_ch_ptr_table', 0x0010A0:'g_ch_word_array',
    0x0010AA:'g_io1_gate',      # TCBIO1I dispatches on this; chassis-supplied
    0x01FFF0:'VMOD_CTRL',       0x01FFF1:'VMOD_CTRL_LO',
}

# ---- task regions, from the TDTI table at $F0A600 ------------------------
REGIONS = [(0xF04488,0xF045FF,'pre-task init - outside every TDTI region; runs before tasks exist'),
           (0xF04600,0xF05CFF,'TCBRDHC  - master dispatch, panel cmds, SLC loader'),
           (0xF05D00,0xF05EFF,'TCBIO1I  - host link, mailbox $70001C/$700020'),
           (0xF05F00,0xF068FF,'TCBXP4I  - XP-32 channel 4  (shifted $18 off the $A00 grid)'),
           (0xF06900,0xF072FF,'TCBXP3I  - XP-32 channel 3'),
           (0xF07300,0xF07CFF,'TCBXP2I  - XP-32 channel 2'),
           (0xF07D00,0xF086FF,'TCBXP1I  - XP-32 channel 1  (the template the other three copy)'),
           (0xF08700,0xF08CFF,'MainInit - self-test dispatcher, two checkpoints'),
           (0xF08D00,0xF09BFF,'Self-test suite - phases $0100-$2903'),
           (0xF09C00,0xF0A5FF,'Reset entry, Phase2Init, RTOSKernelInit, TDTI'),
           (0xF0A600,0xF0A825,'TDTI task definition table, $C0 per entry'),
           (0xF0A826,0xF0FFFF,'free ROM - the monitor is patched here')]

# ---- sites worth a note, from this session's analysis --------------------
_NOTE_PAIRS = [
 # ---- phase $1700 specifies the $400000 BERR gate ----
 (0xF09626,'PHASE $1700 IS THE FIRMWARE\'S OWN SPECIFICATION OF THE $400000 BERR GATE,'),
 (0xF09626,'  and it requires BOTH POLARITIES: with $FF0216 bit 5 SET the probe must'),
 (0xF09626,'  fault (d1 nonzero); with bit 5 CLEAR it must not (d1 zero).  Either way'),
 (0xF09626,'  round the error marker $F0F0F0F0 goes into d7.  The emulator gate'),
 (0xF09626,'  -- serve memory unless data_hi & $20 -- matches exactly.  This is a'),
 (0xF09626,'  VALIDATION, not a correction: the rule was inferred and the firmware\'s'),
 (0xF09626,'  own test confirms it in both directions.'),
 (0xF096AC,'READ PROBE, and the FOUR NOPs ARE LOAD-BEARING.  The temporary bus-error'),
 (0xF096AC,'  handler at $F098E0 resumes at SAVED-PC + 4, but this instruction is only'),
 (0xF096AC,'  2 bytes and the 68000\'s saved PC for a bus error is IMPRECISE -- somewhere'),
 (0xF096AC,'  in or after the faulting instruction.  The NOP padding is slack that makes'),
 (0xF096AC,'  any landing point in that range harmless.  It looks like alignment filler.'),
 (0xF096B8,'WRITE PROBE -- and clr.w matters: a real 68000 READS THE DESTINATION FIRST,'),
 (0xF096B8,'  so on iron this phase exercises the read path too, while the emulator'),
 (0xF096B8,'  (CLR as a pure write) exercises only the write path.  The divergence table'),
 (0xF096B8,'  lists that as a DRAM parity risk; here it changes what a CHASSIS test'),
 (0xF096B8,'  actually covers -- a second, unrecorded consequence of the same gap.'),
 (0xF098E0,'THE TEMPORARY BUS-ERROR HANDLER.  moveq #1,d1 to flag the fault, then'),
 (0xF098E0,'  lea $8(a7),a7 to step over SSW + access address + IR, then'),
 (0xF098E0,'  addq.w #$4,$4(a7) to advance the saved PC by 4, then rte.  That'),
 (0xF098E0,'  arithmetic only lands on the PC low word for the 68000 SEVEN-WORD group-0'),
 (0xF098E0,'  frame -- Musashi had to be patched off its hard-coded 68010 frame for'),
 (0xF098E0,'  exactly this, and with the wrong frame it would fail SILENTLY by resuming'),
 (0xF098E0,'  at a wrong address rather than by faulting.'),
 # ---- the self-test "phase" is a running counter, not a test ID ----
 (0xF08996,'THE PHASE NUMBER IS A RUNNING COUNTER IN d6, NOT A TEST IDENTIFIER.'),
 (0xF08996,'  addi.w #$100,d6 bumps the base between sub-tests, and $F098F0 does'),
 (0xF08996,'  clr.b d6 -- clearing only the LOW byte -- before writing d6 to'),
 (0xF08996,'  CHANNEL_SELECT.  So the high byte is a running base and the low byte'),
 (0xF08996,'  counts steps within a test.'),
 (0xF08996,'  $F098EC is called from EXACTLY ONE site (here) yet produces both $20xx'),
 (0xF08996,'  AND $24xx phases: the outer loop runs a four-sub-test block twice,'),
 (0xF08996,'  advancing the base $400 per iteration.  Confirmed independently by the'),
 (0xF08996,'  beacon PCs being identical between the two groups, and $F099B8 being'),
 (0xF08996,'  called from SIX sites -- exactly why $21xx and $25xx have six phases.'),
 (0xF08996,'  CONSEQUENCE: phase inventories that count distinct beacon values'),
 (0xF08996,'  OVERCOUNT distinct tests.  "30 phases" and "13 confirmed" are counter'),
 (0xF08996,'  values; several groups are re-runs of one block with a different base.'),
 (0xF08A6E,'PHASES $0100-$0168 ARE THE 68000 CPU REGISTER SELF-TEST -- 105 counter'),
 (0xF08A6E,'  values for ONE test, not 105 tests.  First it checks that moveq #$FF'),
 (0xF08A6E,'  sign-extends to $FFFFFFFF (error marker $F0F0F0F0 and spin if not), then'),
 (0xF08A6E,'  walks a value and its complement through EVERY register including the'),
 (0xF08A6E,'  USER STACK POINTER via the privileged move a5,usp / move usp,a3 forms.'),
 (0xF08A6E,'  Nothing chassis-side is involved.'),
 (0xF08AD2,'The USP hop -- reachable only through the privileged move usp forms, so an'),
 (0xF08AD2,'  emulator that stubs USP will fail this phase.'),
 # ---- the panel-code space is partitioned by OWNING TASK ----
 (0xF0568E,'THE PANEL-CODE SPACE IS PARTITIONED BY OWNING TASK, NOT BY CHANNEL.'),
 (0xF0568E,'  Classifying every code by which task regions issue it:'),
 (0xF0568E,'    $258-$260  RDHC ONLY'),
 (0xF0568E,'    $262-$264  XP TASKS ONLY (1-2 sites per task)'),
 (0xF0568E,'    $269-$26C  SHARED -- RDHC and all four XP tasks (the abort/release'),
 (0xF0568E,'               family every task needs)'),
 (0xF0568E,'    $26D-$271  XP TASKS ONLY'),
 (0xF0568E,'    $276-$27B  RDHC ONLY        $27D  RDHC + IO1I'),
 (0xF0568E,'    $27E-$280  IO1I ONLY        $29E-$2A6  RTOS INIT ONLY'),
 (0xF0568E,'  This WITHDRAWS the "per-channel runs of four" idea floated one round'),
 (0xF0568E,'  earlier: each task owns a private block plus one shared group.'),
 (0xF0568E,'  TWO LABEL CORRECTIONS FOLLOW.  $25D-$260, recorded as'),
 (0xF0568E,'  PCMD_CH{1..4}_CONFIG, are RDHC-ONLY with irregular site counts'),
 (0xF0568E,'  (2/1/2/2); a genuinely per-channel code appears once or twice PER XP'),
 (0xF0568E,'  TASK, as $262/$263/$264 do.  The CH1-CH4 labelling is unsupported.'),
 (0xF0568E,'  And $25B (PCMD_CH1_FLUSH) is NEVER ISSUED -- zero sites -- so a named'),
 (0xF0568E,'  code describes something the firmware never does.  $261, $26F and $27C'),
 (0xF0568E,'  are likewise absent, $27C confirming the noted INIT_STEP7 gap.'),
 (0xF0568E,'  CAUTION: a raw byte-pair search is NOT a valid absence test here -- it'),
 (0xF0568E,'  gives 71 hits for $260 and 15 for $258, nearly all incidental matches'),
 (0xF0568E,'  inside displacements and data.  Only the instruction-level sweep'),
 (0xF0568E,'  distinguishes a code being ISSUED from its bytes merely occurring.'),
 # ---- seven undocumented panel codes; RDHC's copy is short ----
 (0xF084AA,'EXTRA ROUTINE PRESENT IN ALL FOUR XP COPIES AND ABSENT FROM RDHC\'s.'),
 (0xF084AA,'  Diffing the five dispatch-subsystem copies over $5C8 bytes: RDHC differs'),
 (0xF084AA,'  from every XP copy by 197 bytes while the XP copies differ from each'),
 (0xF084AA,'  other by only 25-49 -- and most of RDHC\'s difference is that it ENDS'),
 (0xF084AA,'  EARLIER: from $F05C52 to the end RDHC is ALL ZEROS where the XP copies'),
 (0xF084AA,'  carry ~180 bytes of code.  So the subsystem is four copies of a longer'),
 (0xF084AA,'  version plus one shortened variant, not five copies of one thing.'),
 (0xF084AA,'  This routine validates 1 <= channel <= $105E, rejects with panel $263,'),
 (0xF084AA,'  takes the TOP BYTE of d1 (lsr.l #$18) and if nonzero issues DIRECTIVE'),
 (0xF084AA,'  $10 with the task\'s own region base as the parameter block, then writes'),
 (0xF084AA,'  panel code $264 + CHANNEL to the command port.'),
 (0xF084DA,'SEVEN UNDOCUMENTED PANEL CODES, $262-$268, all XP-task-only:'),
 (0xF084DA,'    $262  4 sites, one per task, in the ISR prologue'),
 (0xF084DA,'    $263  4 sites, one per task -- the channel-number reject'),
 (0xF084DA,'    $264  8 sites, two per task, one being THIS addi.w #$264,d1 with'),
 (0xF084DA,'          d1 = the channel, so the family is $265-$268 for channels 1-4'),
 (0xF084DA,'  They fill exactly the gap between the documented $260 and $269, and'),
 (0xF084DA,'  $261 is used NOWHERE -- consistent, since $25D-$260 are the four'),
 (0xF084DA,'  per-channel config codes and a fifth would have no channel.  The panel'),
 (0xF084DA,'  code space is denser than recorded and organised in runs of four.'),
 # ---- the complete XP-task template parameterization ----
 (0xF07E3C,'THE PER-CHANNEL SCAN MASK IS ONE NIBBLE PER CHANNEL.  Guarded on MODE1'),
 (0xF07E3C,'  bit 7 (busy), each XP task loads a mask and calls ITS OWN copy of the'),
 (0xF07E3C,'  channel scan:  XP1I $FFF0, XP2I $FF0F, XP3I $F0FF, XP4I $0FFF.'),
 (0xF07E3C,'  So the scan word holds FOUR 4-BIT PER-CHANNEL FIELDS and each task'),
 (0xF07E3C,'  clears its own -- the mask means "all channels except mine".  A field'),
 (0xF07E3C,'  layout no usage pattern would reveal, and it tells a chassis model the'),
 (0xF07E3C,'  operand is nibble-per-channel rather than a bitmask of flags.'),
 (0xF07E3C,'  (XP4I prints as #$fff, which is why a grep for #$0fff found only three.)'),
 (0xF07E12,'COMPLETE XP-TASK TEMPLATE PARAMETERIZATION, from diffing the four copies.'),
 (0xF07E12,'  ALIGNMENT CORRECTED: XP2I and XP3I align with XP1I at ZERO shift (71 and'),
 (0xF07E12,'  72 differing bytes of 2304); XP4I aligns at -$1E, NOT -$18, with 265'),
 (0xF07E12,'  differing -- and it is unambiguous, the next-best shift gives 2026.  So'),
 (0xF07E12,'  XP4I IS a template copy, its divergence being accumulated patches.  And'),
 (0xF07E12,'  "XP1I/2/3 differ in exactly 77 bytes" is really the XP2I/XP3I pair.'),
 (0xF07E12,'  The patched constant families, XP1I/2/3/4:'),
 (0xF07E12,'    channel literal      01 02 03 04'),
 (0xF07E12,'    BIM CR low byte      44 46 50 52   = $FF0244/46/50/52'),
 (0xF07E12,'    channel +$0E         4E 6E 8E AE   command/status port'),
 (0xF07E12,'    channel +$08         48 68 88 A8   data high'),
 (0xF07E12,'    channel +$0A         4A 6A 8A AA   data low'),
 (0xF07E12,'    record base/+2/+4    66/68/6A ...  = $1066 + (ch-1)*6'),
 (0xF07E12,'    six routine addrs    86 84 83 85 7D 7F  ->  7C 7A ... per task'),
 (0xF07E12,'    scan mask            FFF0 FF0F F0FF 0FFF'),
 (0xF07E12,'  The BIM CR family INDEPENDENTLY RE-DERIVES the documented BIM assignment'),
 (0xF07E12,'  table, irregular step and all (+2, +$A, +2), because the four XP channels'),
 (0xF07E12,'  are not contiguous across the three BIMs.  A diff of four code copies'),
 (0xF07E12,'  reproducing a table built from datasheet reasoning checks both.'),
 # ---- no compiled C; a6 is a structure pointer ----
 (0xF05446,'THE RDHC COMMAND DESCRIPTOR HAS SIX FIELDS, not the three published:'),
 (0xF05446,'    +$00 -> d0   operation code (the PanelStatusDispatch index)'),
 (0xF05446,'    +$04 -> d4   channel, defaulting to $E62'),
 (0xF05446,'    +$08 -> d2   TRANSFER COUNT (BLK_XFR/POLL loop bound d1 = 1..d2)'),
 (0xF05446,'    +$0C -> d1   32-BIT PAYLOAD (what D1_SEND pushes across)'),
 (0xF05446,'    +$10 -> d3   third parameter'),
 (0xF05446,'    +$14 -> a2   THE BUFFER, lea\'d not loaded, so INLINE in the record'),
 (0xF05446,'  Every field lands where a primitive expects it, which is what makes the'),
 (0xF05446,'  layout convincing rather than merely consistent.  A host driving this'),
 (0xF05446,'  machine writes {1, op, channel, count, payload, param, data...} into'),
 (0xF05446,'  chassis memory at $400000.  The op-$14 path is the exception: $F0542C'),
 (0xF05446,'  sets a2 to the fixed ROM address $F0549E instead of the inline buffer,'),
 (0xF05446,'  which is why that operation needs no payload and made a good first probe.'),
 (0xF05370,'THERE IS NO COMPILED C ANYWHERE IN THIS ROM -- not one link/unlk pair in'),
 (0xF05370,'  the whole 64 KB, kernel or application.  Hand-written assembly'),
 (0xF05370,'  throughout, consistent with register-passed parameters, d7 hand-preserved'),
 (0xF05370,'  across a dispatch, swap d0 carrying two values in one register, and'),
 (0xF05370,'  byte-identical template copies no compiler would emit.'),
 (0xF05370,'  But the a6 idiom IS a usable heuristic: a6-relative accesses in the FPS'),
 (0xF05370,'  region are 150 POSITIVE and 0 NEGATIVE -- inverted from a C frame, which'),
 (0xF05370,'  needs negatives for locals.  So a6 is a pointer to a CALLER-SUPPLIED'),
 (0xF05370,'  STRUCTURE, and grouping those accesses by the instruction that loads a6'),
 (0xF05370,'  yields a field map per structure.  That is how the six fields above were'),
 (0xF05370,'  found after only three had been published.'),
 # ---- $FF0212 IS a register: phase $1600 proves it ----
 (0xF09558,'PHASE $1600 IS AN XLTR REGISTER WRITE/READ-BACK TEST, and it settles what'),
 (0xF09558,'  $FF0212 is.  d0 = $10, a0 = $210, then write d0 to (a6,a0.w), a0 += 2,'),
 (0xF09558,'  lsl.b #1,d0 -- so $10 $20 $40 $80 and out on carry: EXACTLY FOUR'),
 (0xF09558,'  registers, $210 $212 $214 $216.  $F095C0 reads them back and branches to'),
 (0xF09558,'  $F095E8 on mismatch, so $FF0212 HAS REAL STORAGE.  The firmware never'),
 (0xF09558,'  uses it functionally, so the documented register table is INCOMPLETE'),
 (0xF09558,'  rather than wrong, and $210-$216 are a four-register group.'),
 (0xF09558,'  Three attempts got this wrong before FPS3K_ACCESSLOG (true CPU-level'),
 (0xF09558,'  widths, added at the user\'s suggestion): the static sweep said "not a'),
 (0xF09558,'  register" (right about use, wrong about existence), the bus log said'),
 (0xF09558,'  "undocumented register" (right, wrong reason), and an adjacency argument'),
 (0xF09558,'  said "longword-split artefact" (wrong -- there are NO 32-bit accesses'),
 (0xF09558,'  anywhere in $FF0200-$FF025F).  Indexed addressing is why no static'),
 (0xF09558,'  search for $212(aN) ever found anything.'),
 (0xF09594,'PHASE $1600 ALSO SPECIFIES WHICH BITS ARE REAL: MODE0 is checked MASKED'),
 (0xF09594,'  $FF (its high byte need not hold what was written) and STATUS_IRQ is'),
 (0xF09594,'  checked masked $610 == $400.  CHANNEL_SELECT, MODE1, COUNTER, IRQ_MASK'),
 (0xF09594,'  and $210-$216 must all read back exactly.  Per-bit rules a chassis model'),
 (0xF09594,'  needs and cannot get from usage patterns.'),
 # ---- the XLTR block by two methods; $FF0212 is an artefact ----
 (0xF0A086,'NOT AN XLTR ACCESS.  $12(a3) here looks like $FF0212 and is not: a3 is'),
 (0xF0A086,'  walking the TDTI TABLE searching for the !TCB marker ($F0A06E loads'),
 (0xF0A06E,'  #$21544342 and $F0A074 compares (a3)), so $12(a3) is a TDTI entry field.'),
 (0xF0A086,'  $FF0212 IS NOT A REGISTER: in the bus log its accesses appear immediately'),
 (0xF0A086,'  after $FF0210 with a different value, because they are the second half of'),
 (0xF0A086,'  a 32-BIT ACCESS to $FF0210 that the logger splits into two words.'),
 (0xF04F3A,'THIS PROVES THE BIM CRs ARE NOT WRITE-ONLY -- a read-modify-write on'),
 (0xF04F3A,'  $FF0230.  A static basic-block sweep reported ZERO reads of every BIM CR'),
 (0xF04F3A,'  because its 80-instruction attribution window cannot reach the ISR'),
 (0xF04F3A,'  prologue where the base register is loaded, jump-table handlers being far'),
 (0xF04F3A,'  from it.  Every absence claim from that method is unsound.  Runtime'),
 (0xF04F3A,'  logging has the opposite flaw -- it INVENTS registers by splitting wide'),
 (0xF04F3A,'  accesses.  Four false positives across two sweeps ($FF0002, $FF0050,'),
 (0xF04F3A,'  $FF00FF, $FF0212), the last from the method just called reliable.'),
 (0xF04F3A,'  What the two methods AGREE on: the channel windows are exactly +$04,'),
 (0xF04F3A,'  +$08, +$0A, +$0E; $FF0214 never appears standalone, only as the leading'),
 (0xF04F3A,'  half of a 32-bit access paired with $FF0216 (which IS used alone 14'),
 (0xF04F3A,'  times); 23 of the 24 BIM registers are used, only $FF025E is spare; and'),
 (0xF04F3A,'  $FF0204 is the hottest register in the machine, 32,968 writes in one run.'),
 # ---- the AP I/F is five identical windows; $FF0010 is invented ----
 (0xF0450C,'THE AP I/F IS FIVE IDENTICAL 32-BYTE WINDOWS.  A basic-block-attributed'),
 (0xF0450C,'  sweep of the BASE window $FF0000-$FF003F finds exactly four offsets --'),
 (0xF0450C,'  +$00, +$04, +$08, +$0E -- the SAME FOUR as each channel window.  So the'),
 (0xF0450C,'  card presents one window type five times: $FF0000 for the host/bulk link'),
 (0xF0450C,'  and $FF0040 + $20N for the four XP channels.  An emulator should model'),
 (0xF0450C,'  one window five times, not two unrelated register blocks.'),
 (0xF0450C,'  $FF0010 (APIF_CMD_ARG_HI) IS NEVER ACCESSED: not as an absolute address,'),
 (0xF0450C,'  not as an attributed displacement, and zero times at runtime across the'),
 (0xF0450C,'  default, RDHC-driven and four-channel configurations, while $FF000E'),
 (0xF0450C,'  reports three in the same runs.  The emulator defines and models it'),
 (0xF0450C,'  anyway -- a register the MODEL invents, like the withdrawn $4F status'),
 (0xF0450C,'  value.  And the emulator name APIF_CMD_ARG_LO for $FF000E is wrong:'),
 (0xF0450C,'  that is the PANEL COMMAND port, written by all eight issuer copies and'),
 (0xF0450C,'  read back as status, exactly like the channel +$0E.  There is no'),
 (0xF0450C,'  command-argument pair; there is one command/status register per window.'),
 (0xF0998E,'THIS IS A RAM-TEST DATA PATTERN, NOT AN ADDRESS.  #$00FF00FF looks like a'),
 (0xF0998E,'  reference into the AP I/F block and is not -- the next instructions are'),
 (0xF0998E,'  not.l d0 (giving $FF00FF00) and move.l #$55AA55AA,d0.  Hex that matches'),
 (0xF0998E,'  an address range is not an address.'),
 # ---- the channel window is exactly four registers ----
 (0xF07EEE,'THE CHANNEL WINDOW IS EXACTLY FOUR REGISTERS -- a systematic sweep of every'),
 (0xF07EEE,'  access through a base register holding $FF0000 (the form absolute scans'),
 (0xF07EEE,'  miss) finds NO fifth per-channel register.  All four windows are'),
 (0xF07EEE,'  perfectly symmetric: the task body writes #$0 to +$04, and the ISR READS'),
 (0xF07EEE,'  +$0E, +$08, +$0A in that order.  So the reconstructed four-register table'),
 (0xF07EEE,'  is COMPLETE.'),
 (0xF07EEE,'  +$0E IS BIDIRECTIONAL: command on write (three tasks send $8000), STATUS'),
 (0xF07EEE,'  on read -- this instruction -- and the word read here is the one then'),
 (0xF07EEE,'  bit-tested for bits 15, 14 and 11.  That is why FPS3K_CHCMD works by'),
 (0xF07EEE,'  supplying a value on that port, and why the local symbol g_ch_block on'),
 (0xF07EEE,'  $1066 is a poor name for a channel STATUS word.'),
 (0xF07EEE,'  AND IT EXPLAINS THE 6-BYTE RECORD STRIDE: $1066+(ch-1)*6 holds exactly'),
 (0xF07EEE,'  {status from +$0E, data-high from +$08, data-low from +$0A} -- three'),
 (0xF07EEE,'  words, six bytes.  The record is the ISR snapshot of its channel window.'),
 (0xF07EEE,'  Trap worth remembering: the first version of this sweep accepted any'),
 (0xF07EEE,'  register that holds $FF0000 SOMEWHERE, so $2(a1) -- a channel data'),
 (0xF07EEE,'  pointer -- counted as $FF0002 with 96 sites, and lea.l -$50(a5),a5'),
 (0xF07EEE,'  reported a nonexistent register at $FF0050.  A sweep written to catch a'),
 (0xF07EEE,'  blind spot introduced two of its own.'),
 # ---- the $10xx per-channel map; $FF004A IS read ----
 (0xF07EFE,'CORRECTION: $FF004A IS READ.  This project states of the value $4F that'),
 (0xF07EFE,'  "it has no connection to $FF004A, which is neither read nor written in a'),
 (0xF07EFE,'  full boot."  The second half is wrong.  Each channel ISR reads BOTH'),
 (0xF07EFE,'  halves of its data pair -- $48(a5) at $F07EF6, already recorded, and'),
 (0xF07EFE,'  $4A(a5) HERE two instructions later -- filing the low half in its'),
 (0xF07EFE,'  per-channel record.  Measured: FPS3K_XPIRQ=1 runs both 468 times, and'),
 (0xF07EFE,'  with a 4-channel chassis all four equivalents ($F074FE, $F06AFE,'),
 (0xF07EFE,'  $F060E6) run 467 times.  The instructive part: this exact claim was'),
 (0xF07EFE,'  ALREADY retracted for $FF0048 on the grounds that absolute-address scans'),
 (0xF07EFE,'  cannot see the $4A(a5) form -- and the neighbouring sentence about'),
 (0xF07EFE,'  $FF004A, same paragraph, same register pair, same instruction sequence,'),
 (0xF07EFE,'  was left standing.  When a method is found blind, EVERY claim resting on'),
 (0xF07EFE,'  it needs revisiting, not just the one that prompted the check.'),
 (0xF08572,'THE $10xx PER-CHANNEL STATE AREA is several PARALLEL arrays, not one'),
 (0xF08572,'  structure.  Stride 4, four entries each: $1080 register-image pointer,'),
 (0xF08572,'  $10AE USER-connect gate, $10BE USER task handle, $10CE and $10DE'),
 (0xF08572,'  per-channel longwords.  Those four sit exactly $10 apart -- 4x4 -- so'),
 (0xF08572,'  $10AE-$10ED is a 4-wide by 4-deep block, confirmed by RTOS init treating'),
 (0xF08572,'  $10EE as the next thing.  Stride 2: $1062 own channel, $1064 the word'),
 (0xF08572,'  array chassis op $A reads back, $10A0 the flag command 1 sets to 2.'),
 (0xF08572,'  Stride 6: the per-channel records at $1066/$106C/$1072/$1078.  Shared:'),
 (0xF08572,'  $105E channel count, $107E a byte all four read, $1098, $10AA.'),
 # ---- THE SEVENTH TASK: 'USER', never created ----
 (0xF08586,'*** THERE IS A SEVENTH TASK, NAMED USER, AND THIS ROM NEVER CREATES IT. ***'),
 (0xF08586,'  #$55534552 is the ASCII literal \'USER\', pushed with a longword and a'),
 (0xF08586,'  pointer for DIRECTIVE $43 -- task-lookup-by-name, the task-level analogue'),
 (0xF08586,'  of $29 for queues.  Exactly four sites, one per XP task: $F06774 $F0718C'),
 (0xF08586,'  $F07B8C $F0858C.  So every XP channel controller tries to resolve a task'),
 (0xF08586,'  called USER, gated on its per-channel longword at $10AE, filing the'),
 (0xF08586,'  answer at $10BE.  TDTI creates only RDHC IO1I XP4I XP3I XP2I XP1I -- but'),
 (0xF08586,'  the name table at $F0467E has SIX entries, XP1I..XP4I then USER TWICE,'),
 (0xF08586,'  and \'USER\' occurs 13 times in the ROM including just before each XP'),
 (0xF08586,'  task entry point.  This fits the FPS-3000 model exactly: the host issues'),
 (0xF08586,'  CPLOAD to load a CONTROL-PROCESSOR PROGRAM, and that program is the USER'),
 (0xF08586,'  task the XP controllers notify on completion.  The ROM is the half of the'),
 (0xF08586,'  system that boots the machine and moves microcode; the other half arrives'),
 (0xF08586,'  from the host.  Consequence for emulation: no chassis model can carry an'),
 (0xF08586,'  XP channel through a full cycle, because the COUNTERPARTY is missing, not'),
 (0xF08586,'  a register.  Hardware prediction: on a real machine running a CP program'),
 (0xF08586,'  $10BE+(ch-1)*4 holds a task handle and $10AE+(ch-1)*4 is nonzero; on this'),
 (0xF08586,'  ROM alone both stay zero.  One RAM dump settles it.'),
 # ---- RESOLVED: $0A is distinguished by d7 ----
 (0xF056C4,'THIS PRESERVES THE OPERATION CODE.  $F0572C does lsl.w #2,d0 to build the'),
 (0xF056C4,'  jump index, destroying the code -- so PanelSendAndWait saves it in d7'),
 (0xF056C4,'  first.  This is the ONLY write to d7 in all of RDHC, so d7 is'),
 (0xF056C4,'  unambiguously the operation code in every handler.  d0 is consumed by'),
 (0xF056C4,'  the dispatch; d7 survives it.'),
 (0xF05AC8,'RESOLVES THE $0A QUESTION.  If MODE1 bit 7 (busy) is clear AND d7 == $0A,'),
 (0xF05AC8,'  POLL takes a DRAIN-TO-COMPLETION path: spin while (a0) bit 15 is set,'),
 (0xF05AC8,'  then check the error bit 13.  Every other POLL code skips this and is'),
 (0xF05AC8,'  re-entered on the next interrupt -- exactly the measured "fires once vs'),
 (0xF05AC8,'  fires 1468 times" split.  So $0A and $14 terminate for DIFFERENT'),
 (0xF05AC8,'  reasons: $14 because it IS D2_FIN, the single finalize slot; $0A because'),
 (0xF05AC8,'  POLL special-cases it here.  The earlier guess -- "the primitives hold'),
 (0xF05AC8,'  d0-dependent early exits" -- was the right class and the wrong register.'),
 # ---- POLL is BLK_XFR's mirror; D1_SEND fire-and-forget ----
 (0xF05A12,'POLL IS MISNAMED -- it is not a status poll, it is the OUTBOUND BULK'),
 (0xF05A12,'  MOVER, the exact mirror of BLK_XFR.  Same swap d0 mode trick, same'),
 (0xF05A12,'  $FF0008 special case (here on the SOURCE), a1 the channel data pair,'),
 (0xF05A12,'  a2 the other end, same one-address-vs-consecutive split.  BLK_XFR moves'),
 (0xF05A12,'  channel -> memory; POLL moves memory -> channel.  So the four primitives'),
 (0xF05A12,'  are TWO MOVERS, A SENDER AND A FINALIZER, and the 9 POLL and 9 BLK_XFR'),
 (0xF05A12,'  slots are the outbound and inbound halves of one operation set.'),
 (0xF05A2C,'THIS IS ONE OF THE SEVEN XLTR_COUNTER SITES, and it gives $20C a role:'),
 (0xF05A2C,'  $04 is written ONLY when the source is the bulk port, so $20C is a'),
 (0xF05A2C,'  BURST/WIDTH COUNTER armed before a bulk read -- which is why $04 is the'),
 (0xF05A2C,'  operational value while $01/$FF are boot-diagnostic only.'),
 (0xF05A3C,'The $218=$400 / poll bit 15 / clear handshake here is the SAME mechanism'),
 (0xF05A3C,'  as the polled bulk loop at $F04AE2 -- one mechanism, two call sites.'),
 (0xF058C0,'D1_SEND FIRE-AND-FORGET EXIT: when d0\'s low word is 4 it returns WITHOUT'),
 (0xF058C0,'  waiting -- unmasking the channel IRQ bit via PanelErrorMaskTable and'),
 (0xF058C0,'  restoring the BIM CR to $5F, so the ISR collects the completion.  This'),
 (0xF058C0,'  is the mechanism class behind the open "$0A terminates but $01 loops"'),
 (0xF058C0,'  question: the primitives hold d0-dependent early exits, so two slots'),
 (0xF058C0,'  sharing a handler need not share its control flow.  Which test'),
 (0xF058C0,'  distinguishes $0A is UNRESOLVED -- $F0572C does lsl.w #2,d0 first, so no'),
 (0xF058C0,'  D1_SEND index satisfies index<<2 == 4.  Recorded open, not guessed.'),
 # ---- BLK_XFR, directives $29/$2A, the S2/S3 handler ----
 (0xF05B0E,'BLK_XFR, THE BULK MOVER.  swap d0 first because ONE REGISTER CARRIES BOTH'),
 (0xF05B0E,'  the dispatch index (low word) and the TRANSFER MODE (high word).'),
 (0xF05B0E,'    d2 = transfer count, loop d1 = 1..d2 inclusive'),
 (0xF05B0E,'    a1 = channel data pair (+$00 high, +$02 low)'),
 (0xF05B0E,'    a2 = destination;  a0 = channel command port'),
 (0xF05B0E,'    mode 0  -> both halves to (a2), a2 NOT advanced'),
 (0xF05B0E,'    mode !=0 -> halves to (a2) and $2(a2), a2 += 4'),
 (0xF05B0E,'  The caller at $F05422 loads d0 = $FFFF000F, so op $14 runs in'),
 (0xF05B0E,'  consecutive-address mode with dispatch index $0F.'),
 (0xF05B1A,'THE DESTINATION MAY BE THE CHASSIS BULK PORT: only when a2 == $FF0008'),
 (0xF05B1A,'  does it wait on $FF0004 bit 0.  So BLK_XFR is a RAM->chassis mover too,'),
 (0xF05B1A,'  and that ready flag is OUTBOUND flow control specifically.'),
 (0xF05B48,'RE-ARMS ONCE PER WORD PAIR, not once per transfer, with a 1000-poll'),
 (0xF05B48,'  timeout each time and $26C RELEASE on expiry.  A chassis model must'),
 (0xF05B48,'  acknowledge bit 14 per pair; per transfer stalls after the first.'),
 (0xF05662,'DIRECTIVE $29 = LOOK UP AN ASQ BY NAME (handle returned in a0), and'),
 (0xF0566C,'DIRECTIVE $2A = POST TO THAT HANDLE.  Two of the fourteen unidentified'),
 (0xF0566C,'  TRAP #1 directives now have meanings, and they are the pair that makes'),
 (0xF0566C,'  RDHC a dispatcher.  The parameter block is {4-byte name, longword,'),
 (0xF0566C,'  word} -- EXACTLY the 10-byte ASQ descriptor layout at TCB+$138.  The'),
 (0xF0566C,'  in-RAM descriptor and the directive argument block are one structure,'),
 (0xF0566C,'  which is why !ASQ needs no tag: the kernel finds them by name.'),
 (0xF055FC,'S2/S3 ADDRESS HANDLER.  d4 = 2 -> one address word, d4 = 3 -> two; d5 is'),
 (0xF055FC,'  the same shift-count mechanism as the S1 handler.  Anything else is'),
 (0xF055FC,'  rejected with panel $260.  Applies the SAME +$10000 staging offset --'),
 (0xF055FC,'  a THIRD implementation after $F051A2 and $F055A2 -- but stores the'),
 (0xF055FC,'  resolved address at $E7E rather than moving data.'),
 # ---- op $5 is XPSEL; long sequences drop their tail ----
 (0xF04F16,'OP $5 IS XPSEL -- IT WRITES THE CHANNEL REGISTER, not just validates.'),
 (0xF04F16,'  These are the ONLY writes to $E60/$E62 in the whole ROM, and $E60 is'),
 (0xF04F16,'  exactly what op $4 validates ($F04E3A) and what RDHC command 1 defaults'),
 (0xF04F16,'  its channel from ($F0537E).  So op $5 is the select-channel primitive'),
 (0xF04F16,'  and the prerequisite for every operation on "the current channel".'),
 (0xF04F16,'  High word $E60, low word $E62, bit 6 selecting as usual ($45 = high).'),
 (0xF05102,'BEWARE LONG FPS3K_SEQ SCRIPTS -- THEY SILENTLY DROP THEIR TAIL.  A 27-code'),
 (0xF05102,'  sequence reached ops 1 2 4 5 8 9 D and not 0 3 6 7 A B C E, which read as'),
 (0xF05102,'  those eight being unreachable.  All sixteen ARE individually reachable:'),
 (0xF05102,'  FPS3K_SEQ=0B:0001 reaches op $B, and so on for every one.  The sequence'),
 (0xF05102,'  simply stops after about FOUR TO SIX codes.  Pacing is NOT the cause --'),
 (0xF05102,'  gaps of 20M, 2M, 500K and 200K cycles and a 3x longer run give identical'),
 (0xF05102,'  results.  The limiter is ARM EVENTS: a code is handed over only when the'),
 (0xF05102,'  SBC issues another panel command, and it stops after a handful.  Put the'),
 (0xF05102,'  "unreachable" ops first and they are the ones that run.'),
 (0xF05102,'  Switching to ONE OPERATION PER RUN took RDHC from 36% to 47% and the'),
 (0xF05102,'  whole-firmware figure from 51% to 54% -- no new hook, no new inference.'),
 # ---- the XP3I outlier resolved ----
 (0xF08616,'THE "XP3I OUTLIER" WAS THE $105E PRESENCE GATE, nothing about the code.'),
 (0xF08616,'  With the emulator default FPS3K_CHANNELS=2 (the real 2-AC machine),'),
 (0xF08616,'  $105E reads 2 and each task\'s cmpi.w #<own channel>,$105E / blt gates'),
 (0xF08616,'  tasks 3 and 4 off before they touch their bodies.  Measured:'),
 (0xF08616,'                CHANNELS=2   CHANNELS=4'),
 (0xF08616,'      XP1I         36.7%        36.7%'),
 (0xF08616,'      XP2I         34.1%        34.1%'),
 (0xF08616,'      XP3I         13.0%        33.8%'),
 (0xF08616,'      XP4I         13.7%        36.5%'),
 (0xF08616,'  All four reach 34-37% once their channel is present -- the 12-to-40%'),
 (0xF08616,'  spread was never a property of the code, and XP4I is not the poor'),
 (0xF08616,'  relation its $18-shifted layout suggests.  The 13% floor is the'),
 (0xF08616,'  SELF-GATED BASELINE: prologue, connect vector, check $105E, park.'),
 (0xF08616,'  CAUTION: for fidelity to this machine CHANNELS=2 is correct and'),
 (0xF08616,'  XP3I/XP4I SHOULD be dormant; CHANNELS=4 exercises code the real box'),
 (0xF08616,'  never runs, so it is a tool for reading the disassembly, not a model.'),
 # ---- the 0% pre-task region resolved ----
 (0xF04500,'PANEL ISSUER COPY 1 IS CORRECTLY DEAD.  It is called from $F001A4 and'),
 (0xF04500,'  its address is stored at $F001A6 -- the hand-placed FPS stub at $F001A0'),
 (0xF04500,'  inside the RMS68K kernel that issues PCMD_KERNEL_FATAL ($2B2) and hangs.'),
 (0xF04500,'  So this copy serves ONLY the kernel panic path; reaching it would mean'),
 (0xF04500,'  the kernel had died.  Its 0% coverage is not a driving gap.'),
 (0xF044A2,'RTOS HOOK, not a called routine: this address is stored as a FUNCTION'),
 (0xF044A2,'  POINTER at $F03FDC and $F040EC.  It tests bit 14 of $0C34, optionally'),
 (0xF044A2,'  saves SR and calls $F01688, then falls into the chain walk below.'),
 (0xF044C0,'DEVICE-DRIVER DISPATCH CHAIN: descriptors linked at +$8 with an entry'),
 (0xF044C0,'  point at +$1E, polled in order until one returns carry set, then jmp'),
 (0xF044C0,'  $F008B6.  This is how a device interrupt would reach a driver.  It never'),
 (0xF044C0,'  runs because this configuration registers NO drivers -- consistent with'),
 (0xF044C0,'  !IDV holding only the six task ISRs and !IOV being empty.'),
 # ---- driving the XP tasks: CHCMD suppresses coverage ----
 (0xF053BE,'RDHC\'s ASQ POST EXECUTES, but it is NOT what wakes the XP task.  The'),
 (0xF053BE,'  gate above (btst #1 of $10A1+(ch-1)*2) is set only by operation $14, so'),
 (0xF053BE,'  the post needs a second pass; forcing the flag makes $F05652 run.  But'),
 (0xF053BE,'  measured, XP1I reaches 34.4% from ITS OWN CHANNEL INTERRUPT ALONE and the'),
 (0xF053BE,'  ASQ post adds nothing.  The hand-off exists and runs; the release is the'),
 (0xF053BE,'  channel BIM interrupt, as before.'),
 (0xF07EB6,'FPS3K_CHCMD=C801 SUPPRESSES COVERAGE, and it was in the config this project'),
 (0xF07EB6,'  recorded as the XP-driven best case (the source of "XP tasks 18-20%'),
 (0xF07EB6,'  each").  Measured, own IRQ alone vs + CHCMD=C801:'),
 (0xF07EB6,'    XP1I 34.4% -> 16.7%    XP3I 12.1% -> 12.1%'),
 (0xF07EB6,'    XP2I 31.4% -> 13.7%    XP4I 12.6% -> 12.6%'),
 (0xF07EB6,'  $C801 sets bits 15, 14 AND 11.  XP1I/2/3 test bit 11 and take the short'),
 (0xF07EB6,'  $8000/$1B branch when it is set; XP4I never tests it, so is unaffected --'),
 (0xF07EB6,'  exactly the asymmetry measured.  The bit added to unblock the channel'),
 (0xF07EB6,'  transaction also SHORT-CIRCUITS the code it was meant to expose.'),
 (0xF07EB6,'  Driving all four channels at once is worse still (7-12% each): the tasks'),
 (0xF07EB6,'  contend, so a config meant to exercise all of them exercises each less.'),
 # ---- executing all 42 operation codes ----
 (0xF05BA8,'THE STATIC CENSUS IS CONFIRMED BY EXECUTION, 13 FOR 13.  Sweeping cmd 1\'s'),
 (0xF05BA8,'  operation code over $00-$29, one emulator run each: every one of the 13'),
 (0xF05BA8,'  slots this table decodes as a bare rts fires NO primitive and gives'),
 (0xF05BA8,'  byte-identical coverage (488 of 5888 RDHC bytes).  The OLD census would'),
 (0xF05BA8,'  have predicted 8 of those 13 to do something.  Live slots each fire'),
 (0xF05BA8,'  exactly the primitive assigned: $01->POLL, $08->BLK_XFR, $14->D2_FIN.'),
 (0xF05BA8,'  Union over all 42 codes: 1326/5888 = 23% of RDHC, from 1% at baseline.'),
 (0xF05A12,'OPEN QUESTION THE SWEEP EXPOSES: $0A and $14 TERMINATE, the other 27 live'),
 (0xF05A12,'  codes do not.  $0A and $14 fire their primitives ONCE; every other live'),
 (0xF05A12,'  code fires 1468 times, once per re-raised interrupt, never completing.'),
 (0xF05A12,'  $14 is the documented finalize code, but $0A is an ORDINARY POLL slot --'),
 (0xF05A12,'  nothing in the static table distinguishes it from $01/$16/$17/$19/$1B/'),
 (0xF05A12,'  $1F/$22/$24, which share this handler and loop forever.  Whatever ends'),
 (0xF05A12,'  the sequence is inside POLL\'s own logic.  Invisible to static reading.'),
 # ---- RDHC's 42-slot table executes; corrected census ----
 (0xF053C4,'CMD 1 PARAMETER BLOCK is {command, operation, channel}: +$00 command'),
 (0xF053C4,'  number, +$04 the OPERATION CODE tested here, +$08 the channel (read at'),
 (0xF053C4,'  $F05372 as $4(a6), defaulting to $E62).  Operation $14 is the path into'),
 (0xF053C4,'  PanelSendAndWait, and FPS3K_CHASSIS_CMD=1,14,1 makes $F0572C EXECUTE --'),
 (0xF053C4,'  RETRACTION: this project recorded "RDHC\'s is the only one of the five'),
 (0xF053C4,'  dispatch copies that never executes, because $F0572C is unreached."'),
 (0xF053C4,'  It is reached, by RDHC\'s own command 1.  D1_SEND, POLL and D2_FIN all'),
 (0xF053C4,'  fire from one command, and RDHC coverage goes 4% -> 19%.'),
 (0xF053B6,'CMD 1 ALSO POSTS TO THE TARGET TASK ASQ BY NAME: #$48585030 = \'HXP0\','),
 (0xF053B6,'  plus the channel number gives HXP1..HXP4, then jsr $F05652.  So this is'),
 (0xF053B6,'  the inter-task path from RDHC to the XP controllers, using the H+name'),
 (0xF053B6,'  host-side convention already seen in the !UST ASQ registry.'),
 (0xF053E8,'THE FIRMWARE DERIVES ITS OWN CHANNEL-WINDOW FORMULA: (ch+1)<<5 + $FF000E'),
 (0xF053E8,'  is the channel COMMAND port and that minus 6 is the DATA pair.  ch=1'),
 (0xF053E8,'  gives $FF004E and $FF0048; ch=2 gives $FF006E and $FF0068.  This is'),
 (0xF053E8,'  independent confirmation of the corrected per-channel table -- +$0E'),
 (0xF053E8,'  command, +$08 data -- which had to be reconstructed from usage patterns.'),
 (0xF05BA4,'CORRECTED CENSUS of the 42 slots (entries are 4EFA jmp d16(pc) pairs):'),
 (0xF05BA4,'    POLL    $F05A12   9 slots  (this project recorded 12 -- WRONG)'),
 (0xF05BA4,'    D1_SEND $F058B2  10 slots'),
 (0xF05BA4,'    BLK_XFR $F05B0E   9 slots  (recorded as 11 -- WRONG)'),
 (0xF05BA4,'    D2_FIN  $F05738   1 slot   at index $14, the only finalize code'),
 (0xF05BA4,'    rts     4E75     13 slots  <-- A FIFTH CASE NEVER LISTED'),
 (0xF05BA4,'  9+10+9+1+13 = 42 exactly.  The old counts summed to 34 of 42 and left'),
 (0xF05BA4,'  eight unexplained.  13 operation codes are unimplemented no-ops,'),
 (0xF05BA4,'  including index $00 -- so this is a SPARSE table, 29 live operations and'),
 (0xF05BA4,'  13 reserved slots: a table sized for a family of machines, of which this'),
 (0xF05BA4,'  configuration implements two thirds.'),
 # ---- CPLOAD runs end to end ----
 (0xF055A2,'S1 RECORD HANDLER -- and this path DEMONSTRABLY RUNS.  With'),
 (0xF055A2,'  FPS3K_RESP=0x94 FPS3K_XPIRQ=6 FPS3K_CHASSIS_CMD=4,8,53310004,0000DEAD,'),
 (0xF055A2,'  BEEF0000 the emulator lands DE AD BE EF at exactly $10010 with nothing'),
 (0xF055A2,'  else in the 55 KB buffer touched -- no monitor L command, no hook writing'),
 (0xF055A2,'  the buffer, no bypass.  Chain: chassis presents $94 in MODE0 and raises'),
 (0xF055A2,'  BIM0 ch0 -> $F04930 latches -> $F0495C -> ISR returns via $F050F8 ->'),
 (0xF055A2,'  RDHC leaves its $13 wait -> $F048D8 sees $14 -> $F052F8 fetches the'),
 (0xF055A2,'  record from $400000 -> cmd 4 -> $F05522 matches S1 -> here.'),
 (0xF055A2,'  The record is BINARY, not ASCII hex: a1 = $10, accumulate address words'),
 (0xF055A2,'  with d5 as the shift count (0 for S1 = one 16-bit word), a1 += $10000.'),
 (0xF055A2,'  So this and $F051A2 are TWO independent implementations of the same'),
 (0xF055A2,'  $10 + addr + $10000 rule and the same $10000-$1FFFF bound -- a stronger'),
 (0xF055A2,'  check on the offset arithmetic than either alone.'),
 (0xF055CC,'HAZARD IN THE FIRMWARE: the bound is the full $10000-$1FFFF, but the live'),
 (0xF055CC,'  TCBs start at $1E900.  A record addressed past $1E8F0 passes this check'),
 (0xF055CC,'  and corrupts RTOS state.  Out of range -> panel $25A.'),
 # ---- what blocks RDHC ----
 (0xF0473E,'*** THIS IS WHAT BLOCKS RDHC. *** Directive $13 is the BLOCKING WAIT, and'),
 (0xF0473E,'  measured over a full boot $F0473C executes EXACTLY ONCE while $F04740'),
 (0xF0473E,'  NEVER executes.  RDHC enters this wait and never leaves it, so the whole'),
 (0xF0473E,'  5,888-byte region behind it -- the four-command host interface, CPLOAD,'),
 (0xF0473E,'  the 42-operation table -- is unreachable.  Not a missing register value'),
 (0xF0473E,'  and not a missing bit: one RTOS wait with no waker.'),
 (0xF04740,'After the wait, bit 7 of the latched MODE0 low byte picks the arm: SET goes'),
 (0xF04740,'  to $F048D8 (the command arm), CLEAR to the 4-bit sub-dispatch at $F0474C.'),
 (0xF048D8,'MISNOMER: the label TCBRDHC_ErrorPath is WRONG -- this is the bit-7 COMMAND'),
 (0xF048D8,'  ARM, the most important arm in the task.  It tests $E86 & $1F:'),
 (0xF048D8,'    $14 -> jsr $F052F8, fetch and execute a command record'),
 (0xF048D8,'    $13 -> moveq #$12,d0 / lea $F0469E,a0 / trap #1  (RDHC directive $12)'),
 (0xF048D8,'  So $14 is the chassis "a command record is waiting" code and the full'),
 (0xF048D8,'  trigger byte is $94: bit 7 for the arm, $14 for the reason.'),
 (0xF0531C,'RDHC READS ITS COMMAND RECORD FROM CHASSIS MEMORY, not SBC RAM: MODE2 is'),
 (0xF0531C,'  saved and forced to page 0, a0 = $400000, then move.l (a0)+,d1.  So the'),
 (0xF0531C,'  host writes command records into shared chassis memory rather than poking'),
 (0xF0531C,'  SBC registers.  Emulator hook: FPS3K_CHASSIS_CMD=<hex longwords>.'),
 (0xF050F8,'MISNOMER: the label ChannelConfigDispatch is WRONG -- this is the ISR EXIT'),
 (0xF050F8,'  STUB (movem / move #$C,ccr / trap #1), the same address as operation $F'),
 (0xF050F8,'  in the 16-entry table.  So every handler\'s "bra.w ChannelConfigDispatch"'),
 (0xF050F8,'  is a return-from-interrupt, and op $F is simply "return immediately".'),
 (0xF050FC,'THE WAKER.  This trap #1 is what would release RDHC\'s directive-$13 wait.'),
 (0xF050FC,'  Measured across every configuration tried (FPS3K_XPIRQ=6, FPS3K_RESP at'),
 (0xF050FC,'  $0B/$07/$0E/$0F/$14/$94, with and without a command record): the ISR'),
 (0xF050FC,'  $F04930 enters ONCE, dispatches ONCE via $F04A6E, and NEVER gets here.'),
 (0xF050FC,'  The operation it selects fails validation, issues a panel command through'),
 (0xF050FC,'  PanelIOConfigure, and that issuer ENDS IN bra . -- so RDHC is blocked by'),
 (0xF050FC,'  the same self-programmed deadlock as TCBIO1I, one level further out.'),
 # ---- RDHC's four-command host interface ----
 (0xF05322,'RDHC HOST COMMAND DISPATCHER.  The command number is the FIRST LONGWORD'),
 (0xF05322,'  of the parameter block in a0 -- move.l (a0)+,d1 -- so each command is a'),
 (0xF05322,'  self-contained record with its arguments following, which is what a host'),
 (0xF05322,'  would write into shared memory.  Validated 1..4, andi #$7, subq #1,'),
 (0xF05322,'  mulu #$6, indexed into the 4-entry jmp-abs.l table at $F05358:'),
 (0xF05322,'    cmd 1 $F05370  attach/configure a channel'),
 (0xF05322,'    cmd 2 $F054A2  read OR write the 16-longword param area at $101E'),
 (0xF05322,'    cmd 3 $F054E8  load d2 longwords from the block into $E8A'),
 (0xF05322,'    cmd 4 $F05502  CPLOAD -- the S-record loader'),
 (0xF05322,'  Out of range -> panel $259 then jmp $F05678.'),
 (0xF054A2,'CMD 2 is BIDIRECTIONAL: d1 is a direction flag, then offset and length;'),
 (0xF054A2,'  a1 = $101E + offset*4, bounds-checked offset+len <= $10 with panel $25B'),
 (0xF054A2,'  on overflow, and exg.l a1,a0 when d1 is nonzero -- so one command both'),
 (0xF054A2,'  reads and writes the channel parameter area, direction chosen by caller.'),
 (0xF05522,'CMD 4 IS CPLOAD.  This dispatches on a 16-bit literal: $5330 $5331 $5332'),
 (0xF05522,'  $5333 = the ASCII pairs S0 S1 S2 S3.  So command 4 is the top-level entry'),
 (0xF05522,'  point of the microcode-upload path this ROM exists to implement, and the'),
 (0xF05522,'  path can be named end to end: host issues RDHC cmd 4 with a count and'),
 (0xF05522,'  S-record text -> $F05502 dispatches by record type -> SRecordDataHandler'),
 (0xF05522,'  at $F051A2 applies the $10 + addr + $10000 offset -> staging buffer ->'),
 (0xF05522,'  chassis op $0 arms the transfer.'),
 (0xF0572C,'CORRECTION: this index is NOT a chassis response code.  $F0572C is the TAIL'),
 (0xF0572C,'  of PanelSendAndWait ($F056BA), reached when the error bit is clear, with'),
 (0xF0572C,'  d0 UNCHANGED FROM ENTRY -- so the index is the CALLER\'s argument, taken'),
 (0xF0572C,'  from a command descriptor: $F0546E does move.l (a6),d0 then cmpi.w #$14'),
 (0xF0572C,'  (D2_FIN), with a6 = a0 on entry at $F05370.  The 42 slots are the SBC\'s'),
 (0xF0572C,'  OWN operation table, each operation implemented by one of the four'),
 (0xF0572C,'  primitives.  This is why sweeping 42 response values never reached here:'),
 (0xF0572C,'  response codes were never the input.  Retires the "streaming-DMA state'),
 (0xF0572C,'  machine, chassis feeds d0 codes" reading.'),
 # ---- the chassis command language: all 16 operations ----
 (0xF05102,'THE CHASSIS COMMAND TABLE, all 16 entries now decoded (was 2 of 16).'),
 (0xF05102,'  The command byte is the LOW BYTE OF XLTR_MODE0, saved at $E86/$E87:'),
 (0xF05102,'    bits 0-3 operation (this table)     bit 5 direction (0 write, 1 read)'),
 (0xF05102,'    bit 4 auto-increment $E7A           bit 6 half select of a 32-bit param'),
 (0xF05102,'    bit 7 selects the OTHER dispatcher $F0495C'),
 (0xF05102,'  So $01/$41 were always op 1 with bit 6 clear/set (address low/high),'),
 (0xF05102,'  and $02/$42 op 2 for the count.  The operations:'),
 (0xF05102,'    $0 F04A84 validate/arm transfer      $8 F04F52 CH1 reset when idle'),
 (0xF05102,'    $1 F04CF2 set address half           $9 F04FA0 set third param $E68/$E6A'),
 (0xF05102,'    $2 F04D20 set count half             $A F04FBA read word array $1064'),
 (0xF05102,'    $3 F04D4E read CHASSIS memory        $B F05002 return staging base $10010'),
 (0xF05102,'    $4 F04E3A validate channel $E60      $C F0502C read long array $1020'),
 (0xF05102,'    $5 F04EE4 validate CHANNEL_SELECT    $D F05092 validate chsel 0..$F'),
 (0xF05102,'    $6 F04F30 read/WRITE SBC RAM         $E F050CA CLEAR BUSY (MODE1 bit 7)'),
 (0xF05102,'    $7 F04F3A mask BIM0 ch0 (IRE)        $F F050F8 end of conversation (RTE)'),
 (0xF05102,'  op $E is the XPRUN clear-busy primitive; op $B is the S-record staging'),
 (0xF05102,'  base, so the chassis can ASK where microcode goes; and op $F is $F050F8,'),
 (0xF05102,'  whose exit stub $F050FC is exactly what !IDV gives for RDHC.'),
 (0xF04D70,'OP $3 -- THE SBC READS CHASSIS MEMORY.  page = addr >> $14 into MODE2'),
 (0xF04D70,'  ($FF0210), offset = (addr & $FFFFF) << 2, read through the $400000'),
 (0xF04D70,'  window.  So MODE2 is the PAGE REGISTER for that window and the window'),
 (0xF04D70,'  is longword-addressed.  This is the SBC read path into System Common'),
 (0xF04D70,'  Memory -- the one direction of the data path that had no mechanism.'),
 (0xF04EC0,'OP $6 WRITE ARM -- THIS IS HOW $10AA GETS WRITTEN.  The chassis sets an'),
 (0xF04EC0,'  address with op $1 and issues op $6; the SBC\'s OWN CPU does the store.'),
 (0xF04EC0,'  Demonstrated: FPS3K_SEQ="01:10AA,06:0002" ->'),
 (0xF04EC0,'    [RAMWATCH] write 0010AA <- 00 from PC=F04EC0'),
 (0xF04EC0,'    [RAMWATCH] write 0010AB <- 02 from PC=F04EC0'),
 (0xF04EC0,'  This SUPERSEDES the bus-master conclusion: that argument\'s premises were'),
 (0xF04EC0,'  right but it was too narrow.  Prior watchpoints missed this because'),
 (0xF04EC0,'  RDHC\'s dispatcher was never driven with op $6 -- the instrument could'),
 (0xF04EC0,'  not fire.  Note $F05E12 reads $10AA as a LONGWORD against 2 while op $6'),
 (0xF04EC0,'  writes 16 bits, so the chassis must target $10AC to set the gate.'),
 (0xF04EB8,'OP $6 READ ARM: bit 5 set returns SBC RAM at $E58 to the chassis in $E74.'),
 # ---- what the eight structures actually hold, read out of RAM ----
 (0xF09F70,'!IDV is the INTERRUPT-DEVICE TABLE and it is the whole IRQ wiring of the'),
 (0xF09F70,'  machine in 84 bytes: six 14-byte records from +$08, each'),
 (0xF09F70,'  {vector word, TCB pointer, ISR entry, ISR exit}.  Measured:'),
 (0xF09F70,'    $45 $1E900 XP1I F07EE6 F07F08     $48 $1EF00 XP4I F060CE F060F0'),
 (0xF09F70,'    $46 $1EB00 XP2I F074E6 F07508     $4A $1F100 IO1I F05DD6 F05E4C'),
 (0xF09F70,'    $47 $1ED00 XP3I F06AE6 F06B08     $41 $1F300 RDHC F04930 F050FC'),
 (0xF09F70,'  The entry column matches the six handlers assembled by hand from the BIM'),
 (0xF09F70,'  vector registers.  The exit column is new -- and $F05E4C in it is the'),
 (0xF09F70,'  address already independently named ISRExit, which pins the field.'),
 (0xF09FA2,'!PAT is a FREE-LIST, not a table: +$04 is a first-record pointer (a third'),
 (0xF09FA2,'  header shape), records chained through their first longword at stride'),
 (0xF09FA2,'  $1E with $FFFFFFFF at +$04.  $1F714 -> ... -> $1F7E6 -> NULL, 8 records,'),
 (0xF09FA2,'  8*$1E + $14 = 254 of the 256-byte page.  The WHOLE page is free, i.e.'),
 (0xF09FA2,'  nothing has ever taken a record from it in any configuration reached.'),
 (0xF09EBE,'!UST is the ASQ NAME REGISTRY.  Rich header: +$0A pages, +$0C record size,'),
 (0xF09EBE,'  +$0E records in use, +$10 first record at base+$14.  Nine $16-byte'),
 (0xF09EBE,'  (task name, ASQ name) pairs: XP1I/AXP1 XP1I/HXP1 XP2I/AXP2 XP2I/HXP2'),
 (0xF09EBE,'  XP3I/AXP3 XP3I/HXP3 XP4I/AXP4 XP4I/HXP4 IO1I/HIO1.  2+2+2+2+1+0 = 9 --'),
 (0xF09EBE,'  a THIRD independent confirmation of the per-task ASQ counts, and the'),
 (0xF09EBE,'  first that is a plain readable list rather than an inference.'),
 (0xF09E78,'!GST uses the same rich header with $D-byte records and ZERO in use.'),
 (0xF09EFE,'Of the eight structures, four are populated (!UST, !IDV, !PAT free list,'),
 (0xF09EFE,'  !VCT) and four are allocated-but-unused (!GST, !IOV, !UDR, $1F500).'),
 # ---- the two untagged structures ----
 (0xF09F1C,'!VCT BUILD.  a2 = $28 (the exception vector table); the loop writes ONE'),
 (0xF09F1C,'  BYTE PER VECTOR up to $400, and the 10 bytes of $FF written just above'),
 (0xF09F1C,'  cover vectors 0-9.  So byte k of the block at $1FA00 IS vector number k.'),
 (0xF09F26,'  $FF marks a vector that differs from the reference handler in a4.'),
 (0xF09F26,'  Later the kernel at $F0226A (directive $4C, connect vector) overwrites'),
 (0xF09F26,'  each connected byte with the OWNING TASK NUMBER.  Measured: $41->6 RDHC,'),
 (0xF09F26,'  $45->1 XP1I, $46->2, $47->3, $48->4, $4A->5 IO1I, and the four orphan'),
 (0xF09F26,'  BIM vectors $42/$43/$44/$49 read 0.  This is the !VCT instance, untagged,'),
 (0xF09F26,'  and it fixes the task numbering the ROM never states: XP1I=1 .. RDHC=6.'),
 (0xF0A030,'RECORD POOL, the other untagged structure ($0C30 -> $1F500).  Instead of a'),
 (0xF0A030,'  marker tag this site writes a two-pointer header: first = base+8, and'),
 (0xF0A030,'  last = first + ((size*256 - 8) div $1A) * $1A -- a divu/mulu pair that'),
 (0xF0A030,'  rounds DOWN to whole $1A-byte records.  One page gives 9 records,'),
 (0xF0A030,'  first=$1F508 last=$1F5F2, which is what RAM holds.  Untagged and'),
 (0xF0A030,'  unchanged in every configuration reached so far; by elimination it is'),
 (0xF0A030,'  !CCB or !DLY, and the $1A record size is the discriminator.'),
 # ---- the RTOS page allocator (TRAP #0 directive $04), 8 sites ----
 (0xF09E78,'ALLOCATOR SITE 1/8 -- TRAP #0 directive $04 is the RTOS PAGE ALLOCATOR.'),
 (0xF09E78,'  Size (in 256-byte pages) goes in via a0; the block comes back in a0.'),
 (0xF09E78,'  Each of the eight sites then: registers the pointer in a directory slot,'),
 (0xF09E78,'  stamps the marker tag at +$00, and writes end = base + (size<<8) - 1 at +$04.'),
 (0xF09E78,'  THIS IS WHY every RTOS structure sits at a $1Fx00 boundary and the TCBs'),
 (0xF09E78,'  stride by $200: the allocation unit is 256 bytes.  -> $0C20, tag !GST'),
 (0xF09EBE,'ALLOCATOR SITE 2/8 -> slot $0C24, tag !UST ($F09ECE), observed $1FB00'),
 (0xF09EFE,'ALLOCATOR SITE 3/8 -> slot $0C66, NO tag stamped, observed $1FA00'),
 (0xF09F42,'ALLOCATOR SITE 4/8 -> slot $0C6A, tag !IOV ($F09F52), observed $1F900'),
 (0xF09F70,'ALLOCATOR SITE 5/8 -> slot $0C6E, tag !IDV ($F09F80), observed $1F800'),
 (0xF09FA2,'ALLOCATOR SITE 6/8 -> slot $0C2C, tag !PAT ($F09FB2), observed $1F700'),
 (0xF09FF0,'ALLOCATOR SITE 7/8 -> slot $0C28, tag !UDR ($F0A000), observed $1F600'),
 (0xF0A020,'ALLOCATOR SITE 8/8 -> slot $0C30, NO tag stamped, observed $1F500'),
 (0xF09F3C,'The beq guard: a zero size means the structure is not created at all.'),
 (0xF09F3C,'  That is the configuration knob -- all eight sizes are pc-relative ROM'),
 (0xF09F3C,'  constants, so the structure inventory is fixed at build time.'),
 # ---- 2026-07-29 second pass: task structure, RTOS surface, channel path ----
 (0xF0A332,'BulkClear: zeroes (d2 << 8) bytes ending at a0, working downward. The loop'),
 (0xF0A33C,'  body is here -- this is the address CLAUDE.md cites for the $10AA zeros;'),
 (0xF0A33C,'  it is not a separate routine. $F0A1D2 is the same shape.'),
 (0xF05652,'issues directives $29 and $2A as a MATCHED PAIR with a stack-built PB (word'),
 (0xF05664,'  $0002, zero longword, d1). RDHC-exclusive; called twice; purpose still open.'),
 (0xF07F56,'post-poll: btst #$D,d4 -- bit 13 = transfer ERROR -> $269 abort + spin.'),
 (0xF07F84,'CHANNEL COMMAND DISPATCH: lsl.w #2,d0 / lea $F083FC,a4 / jmp (a4,d0.w).'),
 (0xF083FC,'42 x 4-byte DISPATCH TABLE, $F083FC-$F084A3 -- the exact twin of RDHC\'s'),
 (0xF083FC,'  PanelStatusDispatchTable at $F05BA4-$F05C4B: same 42 entries, same 4-byte'),
 (0xF083FC,'  stubs, same d0<<2 index, same 4 handlers. Indexed by d0 -- which'),
 (0xF083FC,'  on the acknowledged path is the RETURN VALUE of the trap #1 at $F07F0E'),
 (0xF083FC,'  (directive $0F), NOT a channel command word: all three d0 writes between'),
 (0xF083FC,'  there and here sit on the timeout or error paths. Sixteen indices'),
 (0xF08400,'  onto 4 handlers: $F0810A x10, $F0826A x9, $F08366 x9, $F07F90 x1, and'),
 (0xF08404,'  13 rts entries with no handler. Measured d0 = $0E and $10, so only two'),
 (0xF08408,'  of the 42 indices are exercised. An earlier note said 16 entries and 3'),
 (0xF0840C,'  handlers -- that read only the first 16 and assumed the size from the'),
 (0xF08410,'  WRONG table ($F05102 is the 16-entry one).'),
 (0xF0810A,'channel dispatch handler A -- 9 of the 16 codes land here.'),
 (0xF08366,'channel dispatch handler B -- codes 8 and 9.'),
 (0xF0826A,'dispatch handler C -- indices 1 and 10. NOT executed: needs directive $0F'),
 (0xF0826A,'  to return 1 or 10, which is a KERNEL condition -- no chassis model reaches it.'),
 (0xF0891C,'SELF-TEST CHECKPOINT -- the MOST-CALLED routine in the ROM, 65 calls, all'),
 (0xF0891C,'  from the init/test region: once per subtest. Reads board status $F70018,'),
 (0xF08936,'  tests d7 (the $F0F0F0F0 error flag) and on failure CLEARS VMOD ctrl bit 6'),
 (0xF08940,'  at $1FFF1 and writes $1000 to XLTR MODE1. This is where a failed subtest'),
 (0xF08946,'  becomes externally visible; the beacon at $FF0204 says WHICH subtest.'),
 (0xF04500,'PANEL-COMMAND ISSUER, copy 1 of 8. All 8 are byte-identical over 48 bytes:'),
 (0xF04506,'  stash d0 at $E6E, cmd -> $FF000E, MODE1 b14 clr / b12 set, MODE0 b10 clr,'),
 (0xF04530,'  CHANNEL_SELECT <- d0, then "bra ." -- escape only via F04930 rewriting the PC.'),
 (0xF0467E,'6-entry x 8-byte NAME TABLE: XP1I XP2I XP3I XP4I USER USER. Used by directive $12.'),
 (0xF046B0,"RDHC's directive-$01 block (NOT base+$14 as the other five tasks use)."),
 (0xF046E0,'BIM CR TABLE: $244 $246 $250 $252 = XP1I..XP4I control registers, channel order.'),
 (0xF046F0,'RDHC ENTRY (TDTI +$1C). Prologue is 3 calls: $01, $4C, then $13 -- no ASQ attach.'),
 (0xF04730,'RDHC enables its OWN BIM at LEVEL 6 ($5E). This is why it can never preempt a'),
 (0xF04736,'  level-7 channel ISR -- the deadlock behind the $281 stall. Set by the ROM, not straps.'),
 (0xF04774,'directive $0B, PB at $F04614 (RDHC header +$14). RDHC-exclusive.'),
 (0xF047C0,'directive $0D, PB at $F04630 (RDHC header +$30). RDHC-exclusive.'),
 (0xF04854,'directive $12 x5 from the name table above: XP4I, XP3I, XP2I, XP1I, then USER.'),
 (0xF04884,'  RDHC is the ONLY code in this ROM that addresses the XP tasks by name.'),
 (0xF050FC,'ISR EXIT SEQUENCE, five of six tasks: move #$0C,ccr / trap #1 / moveq #$0F,d0'),
 (0xF050FC,'  / trap #1. RDHC is the exception -- it traps then jmp back into its own code.'),
 (0xF050FC,'ISR EXIT STUB (task descriptor +$10): move #$0C,ccr / trap #1. Identical in all 6'),
 (0xF05100,'  tasks. NOT directive-numbered -- the CCR is the argument; d0 is never loaded.'),
 (0xF053B6,"move.l #'HXP0',d1 then add.b d4,d1 -> HXP1..HXP4. RDHC BUILDS the queue name."),
 (0xF05688,'panel-command issuer, copy 2 of 8 (the "PanelIOCommand processor").'),
 (0xF05DD6,'TCBIO1I ISR. Two arms, selected by mailbox bit 29 at $F05DF8.'),
 (0xF05DF8,'  bit 29 SET -> $F05DFA, issues $281 PCMD_HOST_REQUEST and DEADLOCKS in the spin.'),
 (0xF05E12,'  bit 29 CLEAR -> dispatch on $10AA: 0 -> $282 PCMD_HOST_NULL, 2 -> reply path.'),
 (0xF05E2C,'  reply path: swap + andi #3 on the mailbox word. Class bits 16-17 must read 1;'),
 (0xF05E40,'  0, 2 and 3 write nothing. Verified: class 1 -> reply $00010002 to $700020.'),
 (0xF05E4C,'TCBIO1I ISR exit stub (descriptor +$10).'),
 (0xF05E56,'panel-command issuer, copy 3 of 8 -- the one TCBIO1I calls from inside its ISR.'),
 (0xF05E86,'  THE SPIN. At level 7 the level-6 responder F04930 can never break it: measured'),
 (0xF07D00,'TASK DESCRIPTOR = the whole prologue\'s parameter block. +$08 vector, +$0C ISR'),
 (0xF07D14,'  entry, +$10 ISR exit stub (directive $4C); +$14 directive-$01 block (name,'),
 (0xF07D2C,'  flags, STCK, $190 stack); +$2C and +$36 two 10-byte ASQ entries (AXP1/HXP1).'),
 (0xF07DF6,'PRESENCE GATE: cmpi.w #<own channel>,$105E / blt skip. $105E is the count of'),
 (0xF07DFE,'  channels the chassis presents, written by the CPU at $F0A224. Task n runs if count>=n.'),
 (0xF07E42,'jsr to the channel scan at $F08616.'),
 (0xF07E4C,'command-word dispatch, bit 15 (on $1066 = the ISR\'s snapshot of $FF004E).'),
 (0xF07E86,'  bit 14. Reaching the $8000 path needs bits 15, 14 AND 11 -- verified by sweep.'),
 (0xF07EB6,'  bit 11, the last gate.'),
 (0xF07EC0,'$8000 PATH: data pair <- $0000001B, then $8000 to the command port. Present in'),
 (0xF07ED0,'  XP1I/2/3 only -- 3 sites in the whole ROM. XP4I has no bit-11 test and no $8000.'),
 (0xF07EE6,'XP1I CHANNEL ISR. Snapshots the channel into its 6-byte RAM block:'),
 (0xF07EF6,'  $4E->$1066, $48->$1068, $4A->$106A. NOTE: $FF0048 IS READ, here, as $48(a5).'),
 (0xF07F08,'XP1I ISR exit stub (descriptor +$10).'),
 (0xF07F22,'  then BIM CR $4F (IRE clear), data pair, $8004 REQUEST-TRANSFER, poll.'),
 (0xF08616,'CHANNEL SCAN: move.w $4E(a2,d4.l),d2 / btst #15 / btst #14 / add $20 to d4 /'),
 (0xF0861E,'  loop while d3 <= $105E. Confirms $20 stride and $105E as the channel count.'),
 (0xF086C0,'panel-command issuer, copy 7 of 8 -- the jsr target all over XP1I.'),
 (0xF09BB6,'diagnostics RAM-pattern table: 00000000 FFFFFFFF 55555555 AAAAAAAA.'),
 (0xF0A164,'BIM PROGRAMMING: clears 6 CRs, then writes TEN vector registers $41-$4A across'),
 (0xF0A18E,'  all three BIMs. Six channels are enabled; $42,$43,$44,$49 are vectored but'),
 (0xF0A1CA,'  disabled and have NO handler in any task descriptor.'),
 (0xF0A202,'CHANNEL-PRESENT PROBE: read $FF004E/$6E/$8E/$AE, count the nonzero ones,'),
 (0xF0A224,'  store to $105E. This is the CPU writing $105E -- it is not chassis DMA.'),
 (0xF0A57E,'panel-command issuer, copy 8 of 8 -- THE PANIC PATH. All nine CPU-exception'),
 (0xF0A57E,'  handlers at $F0A23A bra.w here with their code in d0, so the last value at'),
 (0xF0A57E,'  $FF000E names the exception: $29E bus error .. $2A6 catch-all. NOT the TCB'),
 (0xF0A57E,'  table -- that is $F0A600.'),
 (0xF0A600,'TDTI TABLE. Per entry: name +$04, entry point +$1C, region start/end high words'),
 (0xF0A620,'  at +$20/+$22, PROG marker +$40. The six regions partition $F04600-$F086FF exactly.'),
 (0xF04930,'chassis response handler (vector $41). Latches MODE0 -> $E86, sets ack b10,'),
 (0xF04A6E,'  then btst #7,$E87 picks the dispatcher: 0 -> this 16-entry table at F05102'),
 (0xF05102,'16-entry jump table, index = (response & $F) << 2. All 4EFA jmp d16(pc).'),
 (0xF04A84,'code $0: read CHANNEL_SELECT back. $28 -> bulk transfer, else 0..$10 -> $E5C'),
 (0xF04AE2,'INBOUND bulk: arm STATUS_IRQ $400, poll b15, clear, move.w $FF0008 -> (a1)+'),
 (0xF04C50,'OUTBOUND bulk: same port, opposite direction. Selected by $E87 bit 5.'),
 (0xF04B22,'S-RECORD receiver: two ASCII chars per word from $FF0008, "S0"=$5330'),
 (0xF04CF2,'code $1: load destination address half; bit 6 picks high/low'),
 (0xF04D20,'code $2: load word count half; bit 6 picks high/low'),
 (0xF04D4E,'code $3: paged chassis-memory access. MODE2 <- addr>>20; (addr & $FFFFF)<<2 + $400000'),
 (0xF04EE4,'code $5: arg 0 reports $105E (AC count); arg N selects channel N'),
 (0xF04F3A,'code $7: clear IRE (bit 4) of BIM0 CR0 - the chassis can mute the dispatcher'),
 (0xF050F8,'code $F: return from interrupt - the only slot that unwinds the frame'),
 (0xF051A2,'SRecordDataHandler. a1 seeds $10; F051DC adds $10000, so RECORD ADDRESSES'),
 (0xF051DC,'  ARE OFFSETS. Usable record range $0000-$FFEF -> lands $10010-$1FFFF.'),
 (0xF0520E,'the store, guarded by the $10000-$1FFFF check on the COMPUTED address'),
 (0xF05218,'REJECT path: drains $FF0008 until APIF_CMD_STATUS reads <= 0 (end of stream)'),
 (0xF05224,'  ...and only then issues PCMD_CH1_ACK. This is the REJECT exit, not success.'),
 (0xF05230,'SRecordParseLoop: d4 counts the record body down; at d4 == 1 the last byte'),
 (0xF05250,'  (the CHECKSUM) is read into d2 -- and DISCARDED. addq, rts, no comparison.'),
 (0xF05254,'  SUCCESS exit. The firmware does NOT validate S-record checksums; the'),
 (0xF056BA,'PanelSendAndWait. a3 = this channel BIM CR: $4F on entry (IRE off), $5F on exit.'),
 (0xF0572C,'  dispatch on d0 = the latched opcode, into the 42-slot table below'),
 (0xF05BA4,'42-slot dispatch table. FIVE identical copies exist, one per task region;'),
 (0xF05C4C,'  PanelErrorMaskTable: channel -> IRQ_MASK bit (ch1..4 = bits 5,4,3,2)'),
 (0xF05DD6,'TCBIO1I ISR. Reads mailbox $70001C; payload is bits 16-17 of that word.'),
 (0xF05E40,'  reply: mailbox word with bit 1 set, written to $700020'),
 (0xF08732,'btst #5,$F70019 - if SET, skip the ENTIRE self-test suite'),
 (0xF0891C,'PollBoardStatus: bit 4 set AND bit 5 set -> abandon the suite'),
 (0xF08DF8,'BoardStatusPoll_3F11 - polls (status & $3F31) == $3F11. NEVER reads ROM.'),
 (0xF098EC,'phase $2000: RAM address uniqueness, move.l a0,(a0)+ over $0-$10000'),
 (0xF046E0,'4-entry table: XP channel -> BIM control register ($244,$246,$250,$252)'),
 # ---- remaining MainInit / self-test blocks ----
 (0xF08B5C,'phase $0100 tail: bit-manipulation test. 9 bset.b, 5 bclr.b, 1 bchg.b, 1 asl.l,'),
 (0xF08B88,'  each guarded by the $F0F0F0F0 error flag and a PollBoardStatus call.'),
 (0xF08832,'SECOND CHECKPOINT: $1FFF0 <- $D0, then the board-status branch that selects'),
 (0xF08854,'  the next block -- bit4 clear + bit5 clear -> block 2, bit4 clear + bit5 set'),
 (0xF0885A,'  -> block 3. See "Control flow and the two checkpoints" in the access map.'),
 (0xF099EE,'register data-path test: propagate a value d0 -> d1 -> d2 -> d3 -> d4 and'),
 (0xF09A52,'  check it survives. Not a RAM verify despite sitting beside the RAM tests.'),
 # ---- RMS68K segment management ----
 (0xF09D98,'SEGMENT TABLE SEARCH: 10-byte entries -- flags byte at +1, start longword at'),
 (0xF09DA4,'  +2, end longword at +6. Walks until d6, testing whether d2 falls in a range.'),
 (0xF09DB6,'  On no-match, bsr F0A306, the shared error path.'),
 (0xF09E88,'builds the !GST global segment table (tag $21475354) ...'),
 (0xF09ECE,'  ... and the !UST user segment table (tag $21555354)'),
 (0xF0A306,'SHARED ERROR PATH. Saves context to $800 (g_ctx_save). Reached two ways:'),
 (0xF0A30E,'  by direct bsr from the allocator, and as the "BE" bus-error recovery target'),
 # ---- RMS68K bus-error recovery convention ----
 (0xF0A44A,'BUS-ERROR GUARD: pea <recovery>(pc) then move.w #$4245,-(a7). $4245 is ASCII'),
 (0xF0A44E,'  "BE" -- the RMS68K bus error return flag (rms68k_source.SA: PEA KILLER(PC) /'),
 (0xF0A452,'  MOVE.W #\'BE\',-(A7)). The kernel handler finds the marker on the stack and'),
 (0xF0A456,'  resumes at the address below it instead of panicking. Five app sites: F09D0E,'),
 (0xF0A468,'  F0A290, F0A39E, F0A414, F0A44A; plus F00D02/F01F06/F03E40 in the kernel.'),
 # ---- XP task startup (TCBXP1I is the template; the other three copy it) ----
 (0xF07DC2,'XP task startup: a chain of guarded RMS68K syscalls. Each step tests the'),
 (0xF07DC8,'  result and, on failure, loads a PANEL CODE IDENTIFYING THE STEP and aborts'),
 (0xF07DD6,'  via the local panel issuer. $26E = step 1/2, $270 = step 3, $271 = step 4/5.'),
 (0xF07DDE,'  trap #1 with d0 = $4C. RMS68K directive numbers seen here: $4C, $2B, $13,'),
 (0xF07DF6,'  $11 and $0F. RETRACTED: "$0F = 15 = TERM (terminate task)" is a numeric'),
 (0xF07DF6,'  coincidence with the RMS68K source and does not fit. $0F is called exactly'),
 (0xF07DF6,'  once per task, at the TAIL OF THE ISR-EXIT PATH in five of six tasks --'),
 (0xF07DF6,'  terminating the task there would end it on its first interrupt.'),
 (0xF07E00,'  RMS68K source. Note this confirms $26E-$271 are per-STEP, not per-channel.'),
 # ---- RTOSKernelInit ----
 (0xF0A146,'RTOSKernelInit vector fill: install the panic catch-all F0A27A across every'),
 (0xF0A150,'  user vector $124-$3FF -- EXCEPT $230 (vector 140), which is stepped over and'),
 (0xF0A15E,'  keeps the RMS68K generic handler F00896. Whatever raises vector 140 is meant'),
 (0xF0A160,'  to be serviced, not panicked on; which device that is, is not established.'),
 (0xF0A164,'BIM programming: zero six control registers, then load ten vector registers'),
 (0xF0A18E,'  with $41-$4A. Each task later enables its own channel by writing $5F (or $5E'),
 (0xF0A1CA,'  for BIM0 ch0) to its CR -- see the BIM channel table in the access map.'),
 # ---- TCBRDHC host command interface: 4-way dispatch on a 3-bit code ----
 (0xF05344,'HOST COMMAND DISPATCH: andi #7,d1; subq #1; mulu #6; jmp (F05358,d1.w).'),
 (0xF05358,'  4-entry jump table, jmp abs.l per entry. Codes 1-4 only; 5-7 fall into code'),
 (0xF05370,'  cmd 1: attach/configure a channel. Defaults d4 from $E62, validates'),
 (0xF05384,'    1 <= channel <= $105E, then $1080[ch] = &$101E, $10A0[ch] = 2, and'),
 (0xF053B6,'    builds the ASQ name from $48585030 = "HXP0" + channel'),
 (0xF054A2,'  cmd 2: register-file copy to/from $101E. Descriptor = direction, index,'),
 (0xF054D4,'    count; the exg a1,a0 makes one loop serve both directions'),
 (0xF054E8,'  cmd 3: same shape, block at $E8A'),
 (0xF05502,'  cmd 4: start a transfer - load count into $E64, then bset #4 on DATA_HI'),
 (0xF05678,'  shared exit: restore MODE2 from the stack (every handler saves it on entry)'),
 (0xF05684,'  moveq #$F,d0 / trap #1 - RMS68K directive $F'),
 # ---- self-test phase entry points (d6 = phase, written to CHANNEL_SELECT) ----
 (0xF08C4A,'phase $0200: VMOD bit 6 <-> board-status bit 3 inverse wiring'),
 (0xF08D1A,'phase $0300: ROM readback walk $F00000-$F10000'),
 (0xF08D5E,'phase $0400: RAMAddressingTest - no aliasing between $8000 and $10000'),
 (0xF08DF8,'phase $0500: BoardStatusPoll_3F11 - (status & $3F31) == $3F11. NEVER reads ROM.'),
 (0xF08E2E,'phase $0600: VMOD longword pattern walk, 8 patterns from F08E8C'),
 (0xF08F1C,'phase $0700: short-I/O probe at $F82001 - the BUS ERROR is the expected result'),
 (0xF08F70,'phase $0800: IOChannelDiagnostic'),
 (0xF0905A,'phase $0900: PTM interrupt test - vector $150, all three timers latch $0FFF'),
 (0xF08EAC,'phase $1000: MemBusProbe - address-space boundary map'),
 (0xF0918C,'phase $1100: panel bus, VMOD bit 4 -> board-status bit 1'),
 (0xF09236,'phase $1200: level-2 chassis interrupt via vector $14C'),
 (0xF09338,'phase $1300: dual-vector interrupt, vectors $148 and $140'),
 (0xF093CE,'phase $1400: panel-bus interrupt with board-status confirmation'),
 (0xF094F0,'phase $1500: CHANNEL_SELECT must be a clean read/write register'),
 (0xF09518,'phase $1600: XLTR register file walk. STATUS_IRQ b4 = BIM population (16 vs 24)'),
 (0xF09602,'phase $1700: $400000 window access gating via DATA_HI - expects BERR'),
 (0xF096AC,'ChassisProbe_Read: ONE read then four NOPs. The padding exists because a 68000'),
 (0xF096B8,'ChassisProbe_Write: ONE write then four NOPs - same reason: a bus error is'),
 (0xF096C4,'phase $1800: $400000 gating, second pass with other DATA_HI values'),
 (0xF09776,'phase $1900: chassis memory data lines, $55555555 / $AAAA5555'),
 (0xF09832,'phase $1A00: AP I/F window, STATUS_IRQ and COUNTER'),
 (0xF098EC,'phase $2000: RAM address uniqueness, move.l a0,(a0)+ over $0-$10000'),
 (0xF09AD6,'phase $2200: chassis block move, $400000 for $4000 bytes, 4 passes'),
 (0xF09B20,'phase $2300: chassis A14 decode - $400000 and $404000 must be distinct'),
 (0xF04500,'PanelIOConfigure copy 1/7 - pre-task init. Seven BYTE-IDENTICAL copies of'),
 (0xF04530,'  this 50-byte routine exist: F04500 F05688 F05E56 F068A8 F072C0 F07CC0 F086C0,'),
 (0xF05688,'PanelIOConfigure copy 2/7 - TCBRDHC. ...one per task region plus this pre-task one.'),
 (0xF05E56,'PanelIOConfigure copy 3/7 - TCBIO1I (this one spins inside a level-7 ISR)'),
 (0xF068A8,'PanelIOConfigure copy 4/7 - TCBXP4I'),
 (0xF072C0,'PanelIOConfigure copy 5/7 - TCBXP3I'),
 (0xF07CC0,'PanelIOConfigure copy 6/7 - TCBXP2I'),
 (0xF086C0,'PanelIOConfigure copy 7/7 - TCBXP1I'),
]

NOTES = {}
for _a, _t in _NOTE_PAIRS:
    NOTES.setdefault(_a, []).append(_t)

src = open(IN).read().split('\n')

# ---------------------------------------------------------------------------
# Label corrections applied to the frozen input.  fps3k_custom_annotated.asm
# is not regenerable (its own input was a temp file), so wrong labels have to
# be fixed here by substitution rather than at the source.
#
# TCBDefinitionTable was attached to $F0A57E, which is the EIGHTH copy of the
# panel-command issuer, not a table.  The real !TCB table is at $F0A600.  The
# original label was not baseless: RTOS init locates the table FROM $F0A57E by
# rounding up to the next 256-byte boundary ($F09D30: lea $F0A57E,a3 /
# move.l a3,d1 / addi.l #$FF,d1 / clr.b d1  ->  $F0A600).  So $F0A57E is the
# search base and $F0A600 is the table; the label conflated them.
RENAME = {'TCBDefinitionTable': 'PanelCmdIssuer_8'}

# Symbol rename first.  Skipped on ";>>>>" lines: those are unverified Monte
# Carlo prose, and renaming a symbol inside a sentence produces gibberish
# rather than a correction.  One such annotation asserts a TCB definition table
# at $F0A57E and is simply wrong; it is left as written, since ";>>>>" already
# means unverified.
src = [l if l.lstrip().startswith(';>>>>')
       else re.sub(r'\bTCBDefinitionTable\b', 'PanelCmdIssuer_8', l)
       for l in src]

# Whole-line replacements, keyed on the POST-rename text so the rename cannot
# rewrite the replacements themselves.
LINE_FIX = {
 '; DATA   : 0xF0A57E-0xF0B17E  PanelCmdIssuer_8 + 6\u00d796B TCB entries':
   '; DATA   : 0xF0A600-0xF0A840  the real !TCB table - 6 x $60 entries.\n'
   ';          $F0A57E is PanelCmdIssuer_8, NOT a table.  RTOS init locates the\n'
   ';          table FROM it by rounding up: $F09D30 does lea $F0A57E,a3 /\n'
   ';          move.l a3,d1 / addi.l #$FF,d1 / clr.b d1  ->  $F0A600.  The old\n'
   ';          label conflated the search base with the table.',
 '; Coverage: 22988/47992 bytes as code  (47.9%)':
   '; Coverage: inherited from the frozen input and STALE - see CLAUDE.md',
 '; Instructions decoded: 6485':
   '; Instructions decoded: inherited and STALE - see CLAUDE.md',
}
src = [LINE_FIX.get(l, l) for l in src]

out = []
stamp = datetime.date.today().isoformat()
out += [
 '; ============================================================================',
 '; FPS-3000 Control Processor firmware - consolidated disassembly',
 f'; FPS3K_U11_U12_JOIN.bin   MD5 47f133c1c2bab61f887e7e2a92a43dac   built {stamp}',
 '; ============================================================================',
 ';',
 '; THE file to read. Supersedes fps3k_clean.asm. Built by',
 '; tools/mk_consolidated_asm.py from fps3k_custom_annotated.asm.',
 ';',
 '; Symbol names are corrected against the 2026-07-29 findings. Where an',
 '; earlier name was wrong it is NOT preserved, because a half-renamed file is',
 '; worse than either name alone. The notable corrections:',
 ';',
 ';   AP I/F channel window is not write/readA/status/readB.',
 ';     +$08 / +$0A are the HIGH and LOW halves of one 32-bit data register',
 ';     +$0E is a command/trigger register ($8000 fires it)',
 ';     $FF0048 IS read - by the channel ISR at $F07EF6, as $48(a5) with',
 ';     a5=$FF0000. An earlier edition of this header said "never read";',
 ';     that was an absolute-address scan missing the displacement form.',
 ';   $FF0008 is a bidirectional bulk port with three modes (see F04AE2/F04C50/F04B22)',
 ';   $FF0230-$FF025F are BIM registers, not per-channel config',
 ';   $E60 is the XP channel number, not an S-record count',
 ';   $E64 is a transfer word count, not an expected panel response',
 ';   $E74 is the result register returned to the chassis via CHANNEL_SELECT',
 ';   $E86/$E87 hold the latched chassis response, not a "channel mode"',
 ';   F08DF8 is BoardStatusPoll_3F11, not ROMChecksumTest - it never reads ROM',
 ';',
 ';   Added by the second 2026-07-29 pass:',
 ';   $F0A57E is PanelCmdIssuer_8 (copy 8 of 8), NOT TCBDefinitionTable. The',
 ';     real !TCB table is $F0A600; init reaches it by rounding $F0A57E up.',
 ';   $29E-$2A6 are CPU-EXCEPTION reporters, one per class, from a 9-entry',
 ';     table at $F0A23A. All nine bra.w to PanelCmdIssuer_8. If the board',
 ';     dies, the last value at $FF000E names the exception.',
 ';   $105E is a channel-present COUNT written by the CPU at $F0A224, not a',
 ';     chassis DMA target. Each XP task gates on count >= its own channel.',
 ';   The six LIVE TCBs sit at $1E900+$200n - INSIDE the WCS staging buffer.',
 ';     Usable staging is $10000-$1DEFF (~56.75 KB), not the full 64 KB.',
 ';   $F0891C is the self-test checkpoint: 65 calls, one per subtest, and the',
 ';     place a failure becomes visible (clears VMOD bit 6, MODE1 <- $1000).',
 ';   A clean boot issues NO panel command at all - the only $FF000E write is',
 ';     $AAAA, a RAM test pattern. The panel path is error/command-only.',
 ';   Only TRAP #0 and TRAP #1 are used; #2-#15 are free.',
 ';',
 ';   FILE ROLES: fps3k.asm (this file) is the product and the one to read.',
 ';   fps3k_custom.asm is the raw disasm.py output; fps3k_custom_annotated.asm',
 ';   is the frozen build input - it is NOT regenerable (its own input was a',
 ';   temp file), which is why label fixes are applied here by substitution.',
 ';',
 '; Task region boundaries are from the ROM\'s own TDTI table at $F0A600, so',
 '; they are authoritative rather than heuristic.',
 ';',
 '; ANNOTATIONS: lines beginning ";>>>>" are Monte Carlo pass output and are',
 '; NOT verified. Lines beginning ";### " are this session\'s findings, each',
 '; traceable to refs_extracted/versabus_access_map.md.',
 '; ============================================================================',
 '']

def io_name(off):
    return IO.get(off)

cur_region = None
n_notes = n_io = n_ram = 0
for line in src:
    m = re.match(r'^\s*(f0[0-9a-f]{4}):', line)
    if m:
        addr = int(m.group(1), 16)
        for lo, hi, name in REGIONS:
            if lo <= addr <= hi and (lo, hi) != cur_region:
                cur_region = (lo, hi)
                out += ['', '; ' + '='*76,
                        f'; ${lo:06X}-${hi:06X}   {name}',
                        '; ' + '='*76]
                break
        if addr in NOTES:
            for _n in NOTES[addr]:
                out.append(';### ' + _n); n_notes += 1
    # symbol substitution on IO offsets written as $xxx(aN)
    def sub_io(mm):
        # Only 3-digit displacements >= $200 are safe to name: those are the
        # XLTR/BIM file, and no other base register in this firmware is
        # indexed that far.  Short displacements like $8(a5) or $4e(a2) are
        # ambiguous -- the base may be a channel window, a TCB, or a stack
        # frame -- so they are left alone rather than mislabelled.
        global n_io
        off = int(mm.group(1), 16)
        if off < 0x200: return mm.group(0)
        nm = io_name(off)
        if nm: n_io += 1; return f'{mm.group(0)}  [{nm}]'
        return mm.group(0)
    line = re.sub(r'\$([0-9a-f]{3})\(a[0-7]\)', sub_io, line)
    def sub_abs(mm):
        global n_io
        v = int(mm.group(1), 16)
        nm = IO.get(v & 0xFFFF)
        if nm and (v & 0xFF0000) == 0xFF0000: n_io += 1; return f'{mm.group(0)}  [{nm}]'
        return mm.group(0)
    line = re.sub(r'\$(ff0[0-9a-f]{3})\b', sub_abs, line)
    def sub_ram(mm):
        global n_ram
        v = int(mm.group(1), 16)
        nm = RAM.get(v)
        if nm: n_ram += 1; return f'{mm.group(0)}  [{nm}]'
        return mm.group(0)
    line = re.sub(r'\$([0-9a-f]{4,6})\.l\b', sub_ram, line)
    out.append(line)

open(OUT, 'w').write('\n'.join(out))
print(f'wrote {OUT}: {len(out)} lines, {n_notes} finding notes, {n_io} I/O symbols, {n_ram} RAM symbols')
